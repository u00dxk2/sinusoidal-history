# Cold-start primer for 2026-08-20

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 21:45Z (2026-08-19 MT, mid-build handoff)

**If the spectral run was NOT resumed on 8/19 after David's machine restart,
resuming it is today's first-action.** The complete resume sequence (relaunch
command, priority bump, ship checklist) lives in
`docs/cold-starts/2026-08-19.md` § "Mid-build handoff — 15:20 MT". Do not
duplicate-drift it; that section is canonical. Check first whether it already
happened: `git log --oneline -3` — a commit like "Spectral verdict: final run
outputs" means the ship completed; then only the deploy-verify / bus steps
below could remain.

### Today's load-bearing ships (2026-08-19, all pushed, origin/main = 5654566)

- `769d833` — unsmoothed annual TFP companion CSV (`us_tfp_growth_annual.csv`)
  + Fernald refresh; the spectral inference input.
- `cfda495` — **the pre-registration**: frozen `analysis-manifest.yaml` +
  `scripts/spectral_verdict.py` + selftest suite, committed before any results.
- `ad36cdb` — LF-normalized input-CSV freeze hashes (fresh-clone-proof).
- `5654566` — per-cell checkpointing (`tmp/spectral-checkpoint.jsonl`);
  resumed runs byte-identical (verified).

### State of Phase 15 (spectral verdict) at close

- Full selftest **PASSED 12/12 at full sims** (was logged in tmp-selftest.log).
- Headline **confirmed**: 0 of the 9 paired constructions reach the
  3.0-period eligibility gate; all 19 cross-grid cells NO_SIGNIFICANT at dev
  draws (final 99,999-draw p-values pending — the only outstanding compute).
- Page integration is **staged-uncommitted in the working tree** (cycle pages,
  methods + methods.md mirror, llms.txt, AGENTS.md invariant, README,
  CHANGELOG Phase 15, spec build-notes, `src/lib/spectral.ts`, provenance
  source.mds, untracked `public/data/spectral/`). Typecheck / lint / 79-vitest
  / production build all green. It must ship in the same wave as the final
  `public/data/spectral/` outputs — page.tsx imports verdicts.json, so
  committing pages without outputs breaks a fresh clone's build.
- ⚠ NEVER run `--write-manifest` at resume — `ad36cdb`'s manifest is the
  frozen pre-registration; regenerating breaks committed-before-results.

### Pending decision-blocked items

None for this build. Next in queue after Phase 15 ships: **domain cutover to
sinusoidalhistory.com/.org — ⚠ requires David at the pane** (DNS at his
registrar; try Render API with `RENDER_API_KEY`, service
`srv-d7mcat7lk1mc73bidim0`, first), then GSC on the NEW domain (W-001
sequencing: cutover → GSC → sitemap; never verify GSC on the old subdomain).
Also approved + ungated: reuse packets (download-CSV + PNG/SVG export +
attribution + visible license on cycle pages).

### CI status

No CI workflow on this repo (`gh run list` empty — noted, not a red).

### What NOT to touch + why

- `public/data/state-2026.csv` + source.md — frozen edition, never recomputed.
- The v1 API surface — FROZEN per citability research; no new endpoints, no MCP.
- `scripts/spectral/analysis-manifest.yaml`, `verdicts.json`, spectral SVGs —
  never hand-edited; only `spectral_verdict.py` writes them (AGENTS.md
  invariant).
- `cycles.json` file order; fact-checked rationale prose without the cos-math
  audit + same-commit mirror sync.

### Watch items maturing

- **W-001** `nextEvaluation` 2026-08-21: re-run
  `node scripts/crawl-read.mjs --days 14 --snapshot`; if Google still zero at
  14d, the unblock is cutover → GSC on the new domain (already teed up).

### Evangelism / binding-constraint read

Reference artifact; would-tell-a-friend = "this chart made a contested idea
legible enough to argue with." Binding constraint remains DISCOVERY (Google
measured zero; GPTBot crawls daily). The spectral verdict adds the strongest
citability artifact yet: a pre-registered, referee-proof "0 of 9 testable"
number — exactly the quotable-claim shape the LLM-crawler channel rewards.
Retention/word-of-mouth still unmeasurable until GSC-after-cutover; research
prescribes a 60–90-day log baseline before attributing anything. No real-user
advocacy signal today (nothing measurable exists yet).
