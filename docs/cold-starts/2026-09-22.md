# Cold-start primer for 2026-09-22

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-21

<!-- hand:begin banner -->
**Read this before you touch anything.**

1. **FIRST ACTION — walk `/methods` at 390x664 as a cold arrival.** It is the one
   ENTRY page that has never had this read, and it is an entry page on evidence:
   14 of 73 impressions in the first Search Console window landed there, not on
   `/` (`docs/key-user-flows.md` step 1). The same read has now found a real fold
   defect on three consecutive days — `/cycles/<slug>` 09-19, `/cycles` 09-20,
   `/` 09-21 — each on a surface that looked fine on every desktop.

   ```
   node scripts/capture-frame.mjs https://sinusoidalhistory.com/methods docs/frames/2026-09-22-methods-390.png 390 664
   ```

   **Open the PNG before writing a word about it.** `fullPage` is false on purpose:
   a full-page screenshot cannot show a fold, which is the trap all three of this
   week's defects hid in. The narrow question: does a searcher who lands here reach
   the **In brief** answer — what the site is, the spectral headline count, and the
   "See the chart →" link (`src/app/(app)/methods/page.tsx:55-86`) — without
   scrolling? `measure-fold.mjs` now takes a selector, so the answer can be an exit
   code rather than a judgment: that section is `section[aria-label="In brief"]`,
   and it has no `id`, so pass the attribute selector.

2. **`scripts/measure-fold.mjs` takes an optional 4th argument now** (shipped today
   in `adca2e9`): `node scripts/measure-fold.mjs <url> <w> <h> '<css selector>'`.
   With no selector it measures the cycle-page verdict block exactly as before —
   same output strings, re-verified on `/cycles/dalio` today. Use it on any new
   surface instead of writing a throwaway Playwright script, which is what today
   cost before it existed.

3. **`check-rendered-text.mjs diff` cannot see the home page's chart.** It reads the
   static HTML; the chart is client-rendered, so a change to it reads GREEN with
   nothing compared. Measured today, with a positive control: a word change made and
   later reverted did not register either. **For anything inside `Viz`, the proof is
   the rendered `main` innerText line multiset** — snap both sides in a browser, sort,
   compare. Today that was 123 lines and 82 distinct, identical against production.
   For server-rendered prose the tool is still right, and on a re-order it reports RED
   and is right to (that is the 09-20 lesson, unchanged).

4. **W-001's clock is 2026-10-07 and nothing about it moves before then.** The row's
   question is no longer click-through: it asks whether the query set grew beyond the
   five Dalio-dominated rows and whether average position moved off ~21. **Do not run
   the Search Console read looking for news** — it lags ~3 days, so a daily re-read
   returns byte-identical totals, which is the waste the date exists to stop.

5. **Titles, meta descriptions, H1s and URLs stay frozen** until W-001 reads —
   everywhere, not just on the page you are editing. **Layout and body copy are in
   scope**, which is what the last two ships used.

6. **Two rows come due 2026-09-27.** `W-002` (does a phone reader understand a cycle's
   confidence tag now the glossary follows the roster on `/cycles`) — a comprehension
   judgment its readCommand cannot answer, and the row says so. `I-002` (this lane's
   `docs/daily-config.md` declares no `primer:handoff` record, so every morning's
   SessionStart banner reports this primer as having no first action) — **this lane's
   own to fix**, and its `onTrigger` says to read the validator in
   `../skylark-site/src/lib/cc-primer-first-action-blocks.mjs` FIRST: a
   declared-but-invalid record is worse than none, because the generator would then
   own this banner region and could overwrite a hand-authored first action.

7. **`I-001` closed today; its twin `I-003` did not.** The slug alias landed
   (skylark-site `3b23160c4`), so the kickoff's state reads and `agent-status`'s
   verify-receipt gate work now. Still broken, and skylark-site's to fix:
   `check-next-primer-exists` resolves this repo to the workspace id `sinusoidal`
   and finds no report-registry row under it (exit 2 UNRESOLVABLE). **Say that
   plainly at P5 and do not read it as a pass** — this primer's path is asserted by
   this lane's convention, not confirmed by the checker.

8. **Run `verify-with-receipt.mjs -- npm test` BEFORE the first post that claims a
   gate is green,** and put a quoted production read in any sentence that says
   something is live. Both refusals happened today (P3 and P4 first attempts, nothing
   sent either time) and both were avoidable by doing it in the right order.

9. **The ten-on-one-screen build is the next piece of today's work, and it is not a
   quick edit.** The Overlay tab is desktop-only (`hidden sm:inline-flex` in
   `Viz.tsx`), so a phone can show one or two facets at most. A compact ten-row
   overview is the great version of this page's first screen; it needs its own design
   pass, and it is the carried "mobile-overlay decision" from the 2026-08-24 journey
   walk.
<!-- hand:end banner -->

**What shipped (all pushed to `origin/main`):**

- `adca2e9` — **on a phone, the home page now opens on the chart.** The order inside
  `Viz` went from panel → note → charts → brush to **charts → note → brush → panel**,
  moved in the DOM rather than with CSS `order` so reading and focus order match what
  is seen. Curves fully inside the first screen: 0 → 1 at 390x664, 0 → 2 at 1440x900.
  No reader-facing words changed. The same commit gives `measure-fold.mjs` its
  selector argument, fixes the keyboard-focus regression the move created, and updates
  `docs/key-user-flows.md` steps 2-3. CI green.
- `21a136c` — the production 390x664 frame the reached-claim rests on.
- `4841246` — `I-001` closed on its own closeWhen; `I-003` opened for its twin.

**What a fresh agent would otherwise re-derive:**

- **The keyboard fix is load-bearing and was not obvious.** With the summary panel
  below the chart, activating one of its rows scrolls up to the facet, so focus has to
  travel with the scroll or the next Tab lands past the chart. `Viz.tsx`
  `handleSelectCycleFromSummary` focuses the facet's header after the commit, and
  **without** `preventScroll` on purpose: re-picking the already-open cycle changes no
  state, so `FacetView`'s scroll effect does not run for it and the focus call is what
  brings the header on screen. Both halves were found by adversarial review, in that
  order.
- **Delivery evidence on this lane is a content read, not a sha read.**
  `check-deployed-sha-drift --service sinusoidal-history` declines (commit-trigger
  service, `NOT-APPLICABLE-BY-REGISTRY`) — neither a pass nor a read. What stands in is
  `measure-fold` on the production URL, plus a frame.
- **Engineering zero and the report's own numbers** are in
  `docs/daily/2026-09-21-prelaunch.md`; read that rather than re-running the sweeps.

**What NOT to touch:**

- **The banner region above.** Rewritten in place at every close, never appended to.
- **The `Viz` block order and the comment above it.** The comment records why the note
  sits between the charts and the brush, and why the move is in the DOM; a "tidy" that
  moves the panel back to the top re-opens a defect measured on two form factors.
- **`scripts/measure-fold.mjs`'s 390x664 default and its deliberately-red 360x560
  case**, and the no-selector code path — its output strings are compared against
  yesterday's report.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest and
  `verdicts.json` are manifest-frozen (`AGENTS.md`).
- **Frozen `public/data/state-2026.csv`** — as-published edition, deliberately carries
  the pre-`082ce4f` Turchin name.
- **The `--snapshot` series in `docs/crawl-reads.jsonl`** — Render serves only a
  rolling log window, so the snapshot is what makes a read diffable later.
