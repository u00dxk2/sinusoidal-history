# Cold-start primer for 2026-09-27

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — written 2026-09-26 morning (for the day 2026-09-26)

<!-- hand:begin banner -->
**Read this before you touch anything.**

### First action — sinusoidal-cycles, 2026-09-27

`W-002` (does a phone reader understand the NARRATIVE/EMPIRICAL tag on /cycles?) is due
today. Capture its frame, then read it cold and write the verdict into the row's notes:

```
node C:/dev/skylark/sinusoidal-cycles/scripts/capture-frame.mjs https://sinusoidalhistory.com/cycles C:/dev/skylark/sinusoidal-cycles/docs/frames/2026-09-27-cycles-390.png 390 664
```

`I-006` (cycle counts as literals; start at `src/app/og/route.tsx`) is due the same day.

### Carry-forwards

1. Every entry page answers at 320x568, 360x560 and 390x664: 13 of 13 at each
   (`scripts/check-entry-folds.mjs`, production, 2026-09-26 after `4c1228e`). Thinnest:
   `/` 4px at 360x560; `/` and `/cycles/turchin` 12px at 320x568. Re-run all three
   sizes after any edit above an answer.
2. `I-007` (new, due 2026-10-03): `/cycles` descriptions are 14px on phones. Decide 15px
   or keep-with-reason.
3. `W-001` freeze to 2026-10-07: titles, meta, H1 text and URLs stay frozen.
<!-- hand:end banner -->

**What shipped on 2026-09-26 (on `origin/main`, CI green, live on Render 16:31Z):**

- `4c1228e`: every entry page answers on a 320x568 first screen. The change is all
  `max-[360px]:` (width < 360), so 360 and up did not move (12 frame pairs matched
  production pixel for pixel). On `/cycles` the problem was not spacing: the entry name
  wrapped off its colour bar, leaving a 32px empty line.
- The close commit settled `AGENTS.md`: `next dev`'s block is committed as it generates
  it, and the JSX-whitespace section moved below the block's END marker. `next dev` only
  rewrites between its markers (`node_modules/next/dist/server/lib/generate-agent-files.js`,
  `upsertAgentRulesBlock`). Anything this repo must keep goes outside them.

**Non-obvious:**

- `max-[360px]` compiles to `@media not all and (min-width:360px)`, i.e. width < 360.
  `max-[359px]` would leave 359–360 uncovered.
- Killing `npx next start` by its wrapper PID leaves the `next start` child holding the
  port. Stop the whole tree (`taskkill /T /PID <own pid>`), never node by name.
- This session's Git Bash had no PATH (`ls`, `mkdir` and `node` not found). The
  PowerShell tool worked throughout.

**What NOT to touch:**

- **The banner region above**: rewritten in place at every close. Its
  `### First action` heading is load-bearing (the SessionStart hook reads it).
- **`src/lib/overviewReading.ts` and its test**; **`scripts/measure-fold.mjs`'s defaults
  and output strings**; **spectral verdict surfaces** (manifest-frozen);
  **`public/data/state-2026.csv`**.
