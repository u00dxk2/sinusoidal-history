# Cold-start primer for 2026-09-18

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-17

<!-- hand:begin banner -->
**Read this before you touch anything.**

1. **Today's first item is the Search Console read, and it is due 2026-09-19 — not today.**
   Nothing on this lane is blocked, waiting on David, or overdue. If you want the trend
   early it is one command from the lane root:
   `node C:/dev/skylark/sinusoidal-cycles/scripts/gsc-read.mjs --start 2026-09-10`.
   It is a **sample-size check, not a verdict**: at ~4 impressions/day a 09-19 read sees
   n≈28, which is consistent with any true click-through rate up to ~10%. W-001 now
   carries a leading **Branch 0** saying exactly this — read it before applying any
   branch below it. The first genuinely answerable date is ~2026-10-07.
2. **Dependency alerts are at ZERO and the repo runs Next.js 16.3.5** (was 46 alerts and
   16.2.4 yesterday morning). Read it, don't assume it:
   `gh api "repos/u00dxk2/sinusoidal-history/dependabot/alerts?state=open&per_page=100" --paginate --jq 'length'`.
   There is no waiver in force — D-001 was closed and its waiver removed, so a red on the
   engineering panel tomorrow is a NEW fact, not a leftover.
3. **A framework bump makes the CSS bytes change without any style changing.** See KP-004
   in `continuity/items.json` before you read a dirty rendered-output diff as a reason to
   revert: the `@font-face` `url()` tokens are per-build. Normalize them, then compare.
   Three of them contain a dot, which breaks the obvious regex.
4. **The zero you will see is still not a failure.** 17 impressions, 0 clicks, average
   position 20.4. Do not "fix" the snippet on this evidence, and do not change page
   titles, meta descriptions, H1s or URLs without a reason that survives being written
   down — the indexing leg is still fresh and W-001's read depends on it staying
   comparable.
<!-- hand:end banner -->

**What shipped (all pushed, all on origin/main, CI green on each):**

- `44557c1` + `832f4d8` — **`/methods` shows every spectral verdict in one table.** Nine
  rows from the frozen `verdicts.json`, each linking to that cycle page's
  `#does-it-hold-up` block; the unpaired tenth cycle is listed and marked. Live 16:27:45Z
  and 16:37:26Z, verified on production at 390 and 1440 (frames committed under
  `docs/frames/2026-09-17-methods-verdicts-*.png`). `public/methods.md` mirrors it and
  `src/lib/spectral.test.ts` fails if the mirror drifts.
- `a400821` — same-range dependency fixes, 46 alerts → 30, nothing a reader sees changed.
- `7dadae1` — **Next.js 16.2.4 → 16.3.5**, 30 → 0. Ruled by the orchestrator (bus
  `15ac3b4b`) on a verified exposure read, gated on the rendered-output diff. Both pins
  stay exact.
- `e1254f4` — closes D-001 and removes its waiver, because the alerts it covered are gone.
- `0357e54`, `fa40f1e`, `61e9cf5` — primer correction, today's report, the P3 record.

**Two things a fresh agent would otherwise re-derive:**

- **The rendered-output gate lives in the session scratchpad, not the repo.** It snapshots
  the visible text of all 20 prerendered pages plus the emitted CSS and diffs two builds.
  It is what made both dependency ships safe, and it is red-armed. It is NOT committed —
  if you need it again, rebuild it from KP-004's description, and this time consider
  committing it under `scripts/`.
- **The JSX whitespace hazard did not reproduce on 16.3.5** in the two cases probed
  (`AGENTS.md` records it, version-scoped). Keep writing `{" "}`: it renders one space
  either way, every existing junction uses it, and it is the only form correct on both
  versions.

**Open, and owned elsewhere:**

- **This lane declares no primer convention**, so `check-next-primer-exists.mjs` reads
  `UNRESOLVABLE` rather than verifying the file you are reading now. The offset is +1
  (filename date = write date + 1), evidenced by `git log --diff-filter=A` on the last two
  primers: `2026-09-17.md` created 2026-09-16, `2026-09-09.md` created 2026-09-08. The fix
  is a `PRIMER_CONVENTIONS` row in skylark-site, which this lane does not own — surfaced
  in the P5 post.
- **Finding #2 from yesterday's retro** (the dispatch pre-flight prints this lane's
  junction path, `C:/dev/skylark/sinusoidal-history`, instead of the real directory) is
  carried as `skylark-site/2026-09-18/junction-path-preflight` until that session mints a
  real row id.

**David's standing answer, 2026-09-17 (orchestrator `04cff3dc`), and it governs tomorrow's pick.**
Two other lanes reported shipping into an empty room. In his words: *"yes I would rather have more
traffic, but this is okay: … We should keep making all the products excellent and lovable and then
when someone does arrive we make the most of it."* Three consequences here: a missing reading is a
fact about the room, **not** a reason to hold a ship — ship it and write the honest receipt
(`[reached: no]` with the named reason), which is exactly the shape this lane's receipts already
take; do **not** turn 0 clicks into an acquisition push, because distribution is not the constraint;
and "make the most of it" is the actual instruction — **the first person who arrives should meet the
good version.** On this site that means the arrival pages, and the strongest evidence about who
arrives is that `/cycles/dalio` drew 9 of 19 impressions in the last window at position 35.7.

**Scoped, not built:** the same verdict table belongs on `/cycles`. That index lists ten
cycles with no indication that none clears the site's own evidence bar, so a reader who
lands there instead of `/methods` sees the claims without the verdict. It reuses today's
rows wholesale.

**What NOT to touch:**

- **W-001's page-only Search Console read.** Page grouping is the one grouping whose
  totals are whole; a query-grouped read silently undercounts clicks.
- **The `--snapshot` series in `docs/crawl-reads.jsonl`** — Render serves only a rolling
  log window, so the snapshot is what makes a read diffable later.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest and `verdicts.json`
  are manifest-frozen (`AGENTS.md`). Today's table only reads them.
- **Frozen `public/data/state-2026.csv`** — as-published edition, deliberately carries the
  pre-`082ce4f` Turchin name.
- **Per-cycle titles at 76–82 characters** — they truncate from the brand suffix inward,
  which is the right thing to lose.
