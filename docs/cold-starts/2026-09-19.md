# Cold-start primer for 2026-09-19

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-18

<!-- hand:begin banner -->
**Read this before you touch anything.**

1. **Today is W-001's read date.** One command, from the lane root:
   `node C:/dev/skylark/sinusoidal-cycles/scripts/gsc-read.mjs --start 2026-09-10`.
   It is a **sample-size check, not a verdict.** Read **Branch 0 in W-001's
   `nextTrigger.branching`** (`continuity/items.json`) *before* any branch under it: below
   100 post-ship impressions every branch under it is inapplicable, and the disposition is
   to extend the window to ~2026-10-07 and re-read, **not** to escalate. At roughly 4
   impressions a day this read sees about 28. **That exact command was run at the close of
   2026-09-18, 19:44Z, and it works** — it returned `window 2026-09-10..2026-09-18 … TOTAL
   clicks=0 impressions=19 position=21.0`, with `/cycles/dalio` at 9 impressions, position
   35.7, and nine other pages at 1–2 impressions each. That is **identical** to the
   2026-09-17 18:02Z read, which tells you something useful before you run it: Search
   Console's ~3-day lag means no new day had landed in the window yet, so if today's read
   is also 19/0/21.0, the instrument is fine and the sample simply has not grown. The page
   list truncates at the route's row limit; the TOTAL line is whole.
2. **The rendered-text gate is committed now (`e5c53c4`). Do not rebuild it in a
   scratchpad — two sessions did that and lost it both times.**
   `node scripts/check-rendered-text.mjs snap <source> <out.txt>`, then `diff <out.txt>
   <source>`; exit 0 identical, exit 3 changed. A source is a URL, an `.html` file, a
   directory of them (`.next/server/app` after a build), or a saved snapshot. `--selftest`
   carries the red arms and CI runs it through `src/lib/check-rendered-text.test.ts`.
   `AGENTS.md` documents it. Text only: CSS is out of scope (KP-004), layout belongs to
   frames.
3. **The next product candidate is approved as a candidate and NOT started.** Walk
   `/cycles/dalio` at 390px as a cold search arrival and remove whatever stands between
   landing and that page's "Does it hold up?" answer. No code exists for it. The evidence
   that it is the right page is item 1's read: that one page drew 9 of the 19 impressions
   in the last window.
4. **Titles, meta descriptions, H1s and URLs stay frozen until W-001 reads** — on
   `/cycles/dalio` and everywhere else. Changing one makes today's read incomparable with
   the baseline W-001 compares against. Layout and body copy are in scope; the snippet and
   the URL are not.
5. **`agent-status`'s verify-receipt gate reads UNREADABLE for this lane, and that is a
   skylark-site bug, not a missing receipt.** `cc-workspace.json` registers the workspace
   id as `sinusoidal` while the roster slug is `sinusoidal-cycles`, so the gate cannot
   resolve this checkout and does not run. The orchestrator confirmed it and has it on
   their substrate list. No lane action: do not patch it here, and do not read the
   UNREADABLE as evidence that a post failed — but do keep running
   `verify-with-receipt.mjs` before a gates-green claim, because its silence is not a green.
<!-- hand:end banner -->

**What shipped (five commits, all pushed to `origin/main`, CI green on the last of them):**

- `ae830b5` — **the `/cycles` index renders the spectral verdict table above the roster**,
  under "Does any of them hold up?", with a count derived from the frozen `verdicts.json`
  (0 of 9 paired theories clear the 3.0-period floor) and each row linking to that cycle
  page's `#does-it-hold-up` block. The table moved out of `methods/page.tsx` into
  `src/components/VerdictTable.tsx`; both pages render the one component. Live on Render
  16:15:12Z.
- `e5c53c4` — **`scripts/check-rendered-text.mjs`**, the rendered-text gate, with a
  `--selftest` whose red arms are verified, plus `src/lib/check-rendered-text.test.ts`,
  which spawns it in CI. `AGENTS.md` gained the usage block.
- `3e862db` — the P3 record and six production frames under `docs/frames/2026-09-18-*`.
- `777ecef` — W-001's waits gate corrected: the 09-19 read has been an agent read since
  2026-09-16, and the optional Bing Webmaster Tools check is recorded as not-a-wait with
  its re-raise condition.
- `2205904` — the report opener. FIRST ACTION names the day's build; the Search Console
  read sits on a "Due, not today" line, which is the line that comes due today.

**What a fresh agent would otherwise re-derive:**

- **There is no second copy of the verdict table to edit.** Since `ae830b5` both `/cycles`
  and `/methods` render `src/components/VerdictTable.tsx`. The guard that fails if the
  `/methods` rows and `public/methods.md` drift apart is `npx vitest run
  src/lib/spectral.test.ts` (2 passed, 2026-09-18 15:31Z).
- **`/methods` was proved unchanged, not assumed unchanged.** Its production visible text
  before the change and after the deploy is identical at 223 lines, red-armed by mutating
  one verdict cell. That is the same gate now committed as `e5c53c4`, so the next ship
  repeats the proof instead of rebuilding the tool.
- **The frames are production reads.** Each of the six under `docs/frames/2026-09-18-*` is
  cropped to the element whose rows the capture script counted: 10 rows at 390 and 1440,
  0 px of horizontal page scroll, every theory-name link pointing at `#does-it-hold-up`.
- **Nothing on this lane waits on David**, and no board card is open for it
  (`answered-cards --project sinusoidal-cycles`, read 2026-09-18 16:35Z). W-001 is the
  ledger's only open row and its read is a command you can run yourself.
- **Engineering zero, read after the last push:** 0 open Dependabot alerts
  (`gh api "repos/u00dxk2/sinusoidal-history/dependabot/alerts?state=open&per_page=100"
  --paginate --jq "length"`), and `check-engineering-zero --project sinusoidal-cycles`
  reads `0 finding(s), 0 unreadable, nothing to waive`. No waiver is in force.

**What NOT to touch:**

- **W-001's page-only Search Console read.** Page grouping is the one grouping whose totals
  are whole; Search Console anonymizes low-volume queries and a query-grouped read
  silently undercounts clicks.
- **The `--snapshot` series in `docs/crawl-reads.jsonl`** — Render serves only a rolling
  log window, so the snapshot is what makes a read diffable later.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest and `verdicts.json`
  are manifest-frozen (`AGENTS.md`). `VerdictTable.tsx` only reads them.
- **Frozen `public/data/state-2026.csv`** — as-published edition, deliberately carries the
  pre-`082ce4f` Turchin name.
- **Per-cycle titles at 76–82 characters** — they truncate from the brand suffix inward,
  which is the right thing to lose.
- **This banner region.** It is rewritten in place at every close, never appended to, so it
  always states the current day's rules. The kickoff quotes it verbatim.
