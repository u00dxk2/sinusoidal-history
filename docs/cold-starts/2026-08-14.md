# Cold-start primer for 2026-08-14

Multi-session primer — each `/close-session` run appends a new section below.

> **Path note (2026-08-13 evening).** The evening close-out dispatch asked this section to be
> appended to `docs/cold-starts/2026-08-13.md`. No such file existed — `docs/cold-starts/`
> did not exist at all, and the bus shows exactly one message ever posted for this project
> (tonight's task-complete). This repo had **no afternoon rail session today**; its previous
> commit was `38f74ec` on 2026-05-23. So there was no afternoon section to preserve, and a
> file dated for a day that is already over would not be found by tomorrow's agent. Written
> to tomorrow's date instead, per the skill default. Nothing was overwritten.

---

## Session close-out — 04:45Z (2026-08-13 ~22:45 MT) — evening fleet session

### Today's load-bearing ships

- **`448d974` — 8 static per-cycle routes at `/cycles/<slug>`, plus a `/cycles` index.**
  The eight long-wave theories previously existed only as query-param states of `/`
  (`?focus=<id>`), so the `reference_peak_rationale` prose — the most substantive per-cycle
  writing in the project, and the part that survived four fact-check rounds — had nothing
  indexable. Each page now carries period, reference peak, the full calibration rationale,
  the caveat where one exists, confidence classification, source citation, a static SVG of
  the curve, and the paired series with source/license/CSV/provenance links. Plus per-cycle
  `generateMetadata`, a per-cycle OG card at `/og?cycle=<id>`, and JSON-LD
  (WebPage + BreadcrumbList + DefinedTerm + a **Dataset** node for the paired series).
- **`6005b92` — docs.** README (status/structure/sharing), CHANGELOG Phase 12 entry, and a
  new AGENTS.md invariant recording what a new cycle now gets for free versus the two
  hand-written surfaces that will silently go stale.

**Live-verified on production**, not just pushed: all 8 routes 200 with unique titles, all 8
OG endpoints return distinct PNGs, `/sitemap.xml` and `/llms.txt` list all 8, `/cycles` 200s,
unknown slugs 404. Gates before push: build + typecheck + lint + 77 tests (16 new) green.

### Customer impact

A reader who searches for one specific theory — "Kondratiev wave chart", "Turchin secular
cycle data" — can now land on a page about exactly that theory, read why its peak year was
chosen and what that choice concedes, see the data series it is paired with and where that
data came from, and click into the live chart. Before tonight the only entry point was the
eight-cycle overlay, which asks a reader to care about all eight before they can look at one.

### What I deliberately did NOT do

- **No analytics decision.** The dispatch's guardrail was explicit. Noted as a follow-up
  candidate only — now tracked as **W-001** (see below), not left as a prose note.
- **No new historical prose.** Everything on the new pages is reused verbatim from
  `cycles.json`. The peak/trough years each page lists are *derived* from
  `reference_peak_year` + `period_years` by the same cosine the chart draws
  (`peakYearsInRange` / `troughYearsInRange` in `src/lib/cycleRoutes.ts`, unit-tested against
  `sineAtYear`); they agree with `scripts/audit_cycle_rationales.py`. This is structural, not
  incidental — the round-4 finding was that hand-written year-phase claims drift from the
  math, so these pages compute rather than assert.
- **No redesign of the chart, the facets, or the calibration panel.** Untouched.

### Carried thread (honest — this did not land)

**W-001 · Zero analytics on the site.** No verified Google Search Console property, no
PostHog, no page-view counter. So "did the 8 new routes earn impressions?" is unanswerable,
and any future distribution work here is blind. *Close condition:* a GSC property verified for
`sinusoidal-history.skylarkcreations.com` **and** `/sitemap.xml` submitted through it — or an
explicit David decision that this site stays uninstrumented, recorded so it stops being
re-raised. Next evaluation 2026-09-13. Full detail in `continuity/items.json`.

### Tomorrow's first action

Nothing is blocked and nothing is half-built — the evening ship is complete and live. If you
want the highest-leverage next move on this project, it is **W-001**, and it is a David call
before it is an engineering task: decide whether this site gets a GSC property at all. If yes,
verification is one DNS TXT record or one HTML meta tag, then submit the already-live sitemap.
If no, close W-001 as declined so it stops recurring.

If you are here for engineering work instead, the honest read is that this project has no open
engineering need tonight. Do not invent one — see "what NOT to touch".

### Pending decision-blocked items

- W-001 (analytics) — needs David's yes/no on instrumenting the site. **Not currently on his
  board and not posted as a needs-decision**, deliberately: it is a P2 on a portfolio artifact
  with no revenue, and it did not warrant interrupting him tonight. Raise it at the next
  natural touchpoint rather than as a standalone ask.

### Active in-flight work

None. No Codex jobs, no queued deploys, no open branches. Working tree clean, HEAD pushed.

### CI

**No CI.** This repo has no `.github/workflows/` and `gh run list` returns empty. Verification
is local-only (`npm run build` + `npm run typecheck` + `npm run lint` + `npm test`) plus a live
fetch against production after Render auto-deploys. If you ship here, you run those four
yourself — nothing else will.

### What NOT to touch tomorrow, and why

- **`cycles.json` / `series.json` prose.** Every year-claim in there went through four external
  fact-check rounds. Do not reword, tighten, or "improve" it. If you must change a rationale,
  `python scripts/audit_cycle_rationales.py` is mandatory first (AGENTS.md).
- **Cycle `id` values.** Since Phase 12 an id determines a public URL
  (`strauss_howe` → `/cycles/strauss-howe`). Renaming one is a redirect-worthy URL change.
- **The prose mirrors.** `public/about.md`, `public/methods.md`, `public/colophon.md` must be
  edited in the same commit as their React counterparts. This has drifted three times before.
- **`public/llms.txt` per-cycle list and the `about.md` mirror block** — these are the two
  surfaces a new cycle does NOT update automatically. Sweep them by hand or they go stale.

### Watch items maturing within 24h

None. W-001's next evaluation is 2026-09-13.

### Binding-constraint status

**Discovery, and it is unmeasured.** The site is a finished, fact-checked artifact with no
acquisition path and zero instrumentation. Tonight's ship built the surface that could earn
search traffic; nothing yet can observe whether it does. Under the evangelism frame: the
would-tell-a-friend moment is "this chart made a contested idea legible enough to argue with,"
and the distance from it is unknown — there is no retention instrument, no review surface, no
share tracking, and no real-user advocacy signal has ever been recorded for this project. The
move that changes that is W-001, not more product.

### Supersedes

Nothing. This is the first session close-out recorded for this project.
