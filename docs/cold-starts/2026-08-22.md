# Cold-start primer for 2026-08-22

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 21:20Z (2026-08-21 ~15:20 MT)

### The headline: GOOGLE ARRIVED. Do NOT re-run the 8/22 W-001 read — it already ran.

The W-001 read staged for today (8/22) was executed a day early on 8/21 (two
post-cutover poll windows had passed; same early-read precedent as 8/15).
Both staged questions are answered — commit `2f766eb`:

- **Google-family: 731 hits in the 14-day window vs ZERO in every prior read.**
  Googlebot fetched sitemap.xml 2026-08-20 22:18Z — hours after David submitted
  it through the new GSC property — polled robots.txt near-hourly overnight, and
  content-crawled the `/cycles/<slug>` routes, the spectral SVGs, and the paired
  CSVs through 8/21 17:08Z. Google-other (543 hits, likely
  GoogleOther/InspectionTool onboarding the new domain) swept every page
  including `/llms.txt`.
- **The LLM channel survived the cutover** (the urgent branch is dead):
  OAI-SearchBot observed following the old-host 308 to a 200 same-second;
  GPTBot crawled all 10 cycle routes + spectral outputs + the v1 API in-window.
- Snapshot appended to `docs/crawl-reads.jsonl` (complete=true, buckets sum).

### Today's load-bearing ships (both pushed; origin/main = `2f766eb`)

- `2f766eb` — the W-001 read above + instrument fix: `scripts/crawl-read.mjs`
  now backs off on HTTP 429 and retries the page (Render allows 30 req/min; a
  14-day read paginates ~119 pages — three consecutive runs had died
  mid-pagination before the fix). Selfcheck passes. W-001 re-dated.
- `1cd4a66` — primer Session-2 back-fill + `.playwright-mcp/` gitignored.

### Tomorrow's first-action

Nothing matures on 8/22. No project gate, no watch, no David-blocked item.
If running /daily: normal pass; the one standing date is below.

### Watch items maturing

- **W-001 `nextEvaluation` 2026-09-03** — now the GSC INDEXING question, not
  "is Google present" (answered). Read the GSC coverage report on
  sinusoidalhistory.com (David's GSC, hello@skylarkcreations.com): are
  /cycles + the 10 routes indexed, any impressions/queries? Pair with
  `node scripts/crawl-read.mjs --days 14 --snapshot` (needs `RENDER_API_KEY`;
  takes a few minutes now due to rate-limit backoff waits). A zero-impressions
  read before 9/03 means nothing — fresh 14-day window from the 8/20 verify.

### CI status

No CI workflow on this repo (`gh run list` empty — noted, not a red).

### Session-ops note

The bus listener died once today (~19:37Z, dead pid behind a surviving
pidfile) and was restarted cleanly via /listen. Fresh pane tomorrow: /listen
as usual; the pidfile reaper handles any stale state.

### What NOT to touch + why

- `public/data/state-2026.csv` + source.md — frozen edition, never recomputed.
- The v1 API surface — FROZEN per citability research; no new endpoints, no MCP.
- `scripts/spectral/analysis-manifest.yaml`, `verdicts.json`, spectral SVGs —
  only `spectral_verdict.py` writes them; NEVER `--write-manifest` post-hoc.
- `cycles.json` file order; year-phase prose without the cos-math audit +
  same-commit mirror sync.
- Do NOT re-create a GSC property or re-submit the sitemap — done 8/20, working.

### Evangelism / binding-constraint read

Binding constraint is still DISCOVERY, but it materially moved: both engines
now crawl the site (Google arrived 8/20; GPTBot/OAI daily since 8/07). Still
ZERO evidence any human has ever loaded the site — the browser-ish buckets
remain unattributable, and impressions/queries wait on the 9/03 GSC read.
Would-tell-a-friend moment unchanged: "this chart made a contested idea
legible enough to argue with." No real-user advocacy signal yet; retention/
word-of-mouth unmeasurable until GSC data accrues (research prescribes a
60–90-day baseline).
