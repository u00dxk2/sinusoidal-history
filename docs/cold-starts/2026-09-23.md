# Cold-start primer for 2026-09-23

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-22

<!-- hand:begin banner -->
**Read this before you touch anything.**

### First action — close `I-002` from the banner you were just handed

`I-002` (this lane's primer declared no machine-readable first action, so every
morning's SessionStart banner said so) is dated **2026-09-23 — today** and its read
is the banner at the top of your own context. This primer is the first one written
with a `### First action` heading inside the `hand:begin banner` region, which is
what the hook reads (`~/.claude/hooks/sessionstart-mt-date-and-primer.mjs:49-56`,
rule 2: the first ATX heading matching `/first[\s-]+action/i`, ending at the next
heading of the same or higher level — here `### Carry-forwards`).

**So read your own SessionStart text first.** If it carries this section instead of
"declares NO first-action section", `I-002`'s closeWhen is met: close the row and
link `ac42fcf` (the re-scope) plus tonight's primer commit. If you cannot tell from
the injected text, run the read directly:

```
node C:/Users/david/.claude/hooks/sessionstart-mt-date-and-primer.mjs
```

It prints what SessionStart would inject. **No "declares NO first-action section"
line = fixed.** Still there = NOT fixed: read `firstActionHeadingLevel` and
`fenceMask` in that hook before changing this file again, and do **not** fall back to
writing a `primer:handoff` record in `docs/daily-config.md` — that leg was ruled out
on 2026-09-22 (bus `da93e347`), because a declared-but-invalid record lets the
generator own this banner region and overwrite a hand-authored action.

### Carry-forwards

1. **The day's product question is the narrow-phone extreme, and it is a real
   decision, not a bug.** Today's ship put 10 of 10 curves inside a 390x664 first
   screen; at 360x560 it is 6 of 10 and at 320x568 it is 5 of 10 (both from 0). "All
   ten" is a 390-wide promise. Before building anything, decide whether a 320px
   reader is a reader this site has — nothing in this lane can answer that, so it is
   a judgment to make in one line and record, not a measurement to chase.
2. **`scripts/measure-fold.mjs` takes a CSS selector as its 4th argument, and the
   selector must cover the QUESTION, not the container.** `/methods` read RED on
   2026-09-22 for `section[aria-label="In brief"]` because that section also holds
   the jump list, while the answer itself was fully visible with 57px to spare. A
   container-scoped RED is not a defect; it is a badly aimed instrument.
3. **`src/lib/overviewReading.ts` is the single predicate behind both readings of an
   overview row** — the drawn one (now-line, dot) and the spoken one (sr-only text).
   They drifted apart in the morning ship and were re-joined in `258c538`.
   `src/lib/overviewReading.test.ts` pins it, red-armed. Do not re-introduce a second
   copy of "is the current year inside the range".
4. **`check-rendered-text.mjs diff` cannot see the home page's chart.** It reads
   static HTML; `Viz` and everything under it is client-rendered, so it reports GREEN
   having compared nothing. For anything inside `Viz` the proof is the rendered `main`
   innerText line multiset — snap both sides in a browser, sort, compare. Today: 150
   lines, 98 distinct, 0 lines only-in-either, built vs production.
5. **`W-001`'s clock is 2026-10-07 and nothing about it moves before then.** Its
   question is no longer click-through: it asks whether the query set grew beyond the
   five Dalio-dominated rows and whether average position moved off ~21. **Do not run
   the Search Console read looking for news** — it lags ~3 days, so a daily re-read
   returns byte-identical totals.
6. **Titles, meta descriptions, H1s and URLs stay frozen** until that read —
   everywhere, not just on the page you are editing. **Layout and body copy are in
   scope**, which is what the last three ships used.
7. **`W-002` is due 2026-09-27** (does a phone reader understand a cycle's confidence
   tag now the glossary follows the roster on `/cycles`) — a comprehension judgment
   its readCommand cannot answer; it needs a cold read of a frame.
8. **Two substrate items are rowed and dated, not carried as prose:** declaring the
   two leading product numbers in `docs/daily-config.md`, and the roster wrapper over
   `measure-fold.mjs` whose first run is their receipt. See the ledger.
9. **Codex was RED all of 2026-09-22** (`codexCalls: 0 (probe-red)`). The adversarial
   review went to a Claude subagent and found four real defects; the orchestrator's
   review found a fifth. Re-probe before assuming either colour.
<!-- hand:end banner -->

**What shipped (all pushed to `origin/main`, CI green on each):**

- `57ca745` — **on a phone, the home page's first screen now shows all ten cycles.**
  A new `CycleOverview` figure above the tabs, phone-only, on the facets' shared axis
  with a dot at 2026. Curves inside a 390x664 first screen: 1 → 10, each row measured
  by its own selector on production. The phone dek lost "Tap a row to focus and
  calibrate." because the rows are a figure, not controls (24px vs the 44px tap floor);
  the instruction now sits in the figure's caption and renders only on the Facets tab.
- `386ddd7` — the production frame the reached-claim rests on.
- `258c538` — the spoken reading of a row now moves with its drawn one, behind one
  shared predicate and a red-armed test.
- `f8a914b` — `I-003` closed on this lane's own read; the `/methods` phone frame.
- `ac42fcf` — `I-002` re-scoped to the banner leg and re-dated to today.

**What a fresh agent would otherwise re-derive:**

- **Un-hiding the desktop Overlay tab is not the mobile answer.** Ten overlaid curves
  in 358px is a tangle; small multiples are the readable form. That decision closed the
  carried journey-walk gap "mobile has no Overlay tab while the H1 promises one axis"
  (2026-08-24, J-deferred #2) — see `docs/key-user-flows.md`.
- **Axis labels are filtered on PLACED positions, greedily, now-label first**
  (`CycleOverview.tsx`). Filtering on raw positions while the renderer clamps edge
  labels inward is what made 1600 and 1700 collide at 320px. Re-measured across 11
  width/range cases after the fix: no overlap, smallest gap 7.2px.
- **Delivery evidence on this lane is a content read, not a sha read.**
  `check-deployed-sha-drift --service sinusoidal-history` declines
  (`NOT-APPLICABLE-BY-REGISTRY`, commit-trigger service) — neither a pass nor a read.
  What stands in is `measure-fold` on the production URL plus a committed frame.
- **Today's numbers and their commands** are in `docs/daily/2026-09-22-prelaunch.md`;
  read that rather than re-running the sweeps.

**What NOT to touch:**

- **The banner region above.** Rewritten in place at every close, never appended to —
  and as of today its `### First action` heading is load-bearing: it is what the
  SessionStart hook reads.
- **`src/lib/overviewReading.ts` and its test** — the single predicate behind a row's
  two readings.
- **The `Viz` block order and the comments above it.** They record why the chart leads,
  why the note sits between the curves and the brush, and why the overview is
  phone-only.
- **`scripts/measure-fold.mjs`'s 390x664 default and its deliberately-red 360x560
  case**, and the no-selector code path — its output strings are compared across days.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest and
  `verdicts.json` are manifest-frozen (`AGENTS.md`).
- **Frozen `public/data/state-2026.csv`** — as-published edition, deliberately carries
  the pre-`082ce4f` Turchin name.
- **The `--snapshot` series in `docs/crawl-reads.jsonl`** — Render serves only a
  rolling log window, so the snapshot is what makes a read diffable later.
