# Cold-start primer for 2026-09-20

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-19

<!-- hand:begin banner -->
**Read this before you touch anything.**

1. **FIRST ACTION — walk `/cycles` at 390x664 as a cold arrival, the same read that
   found yesterday's defect, pointed at the page a reader reaches when they do not
   already know which theory they want.** Start by capturing what they see without
   scrolling:

   ```
   node scripts/capture-frame.mjs https://sinusoidalhistory.com/cycles docs/frames/2026-09-20-cycles-390.png 390 664
   ```

   That command ran yesterday against `/cycles/dalio` and produced the two committed
   frames; only the URL and the output path change. **Open the PNG before writing a
   word about it.** `fullPage` is false on purpose — a full-page screenshot cannot
   show a fold, which is exactly the trap yesterday's defect hid in.

2. **W-001's clock is at 2026-10-07 and today is not its day.** Do not run the
   Search Console read looking for news: it was read 2026-09-19 15:09Z
   (`node scripts/gsc-read.mjs --start 2026-09-10` → `TOTAL clicks=0 impressions=19
   position=21.0`) and re-dated under the row's own Branch 0. Three consecutive
   daily reads returned byte-identical totals, because Search Console lags ~3 days
   and the window cannot grow faster than the lag. **A paragraph written about an
   unchanged read is the waste this date exists to prevent.**
3. **If you do read it early, and it is under 100 post-ship impressions, DO NOT
   EXTEND THE DATE AGAIN.** The measured rate is ~2.7 impressions/day, so 10-07
   projects to ~68 — the date is at the optimistic edge of the floor, and a second
   extension is exactly what the waits gate forbids. The row already carries the
   answer: convert to the questions answerable at that n (did the query set grow
   past the five Dalio rows, did average position move off ~21). CTR reopens as a
   fresh row only if position moves under 20.
4. **Titles, meta descriptions, H1s and URLs stay frozen** until W-001 reads —
   everywhere, not just on the page you are editing. The freeze protects the
   comparison the row is built on. **Layout and body copy are in scope**, which is
   what yesterday's ship used. When the row converts under item 3, the freeze lifts
   with it; do not let it outlive the read it protects.
5. **The fold check is committed — use it, do not rebuild it.**
   `node scripts/measure-fold.mjs <url> [width] [height]` exits 1 when a cycle
   page's "Does it hold up?" answer is cut. Defaults to 390x664, which is what an
   iPhone 14 in Safari actually shows. **The 844px CSS viewport is not the fold**
   and reading it is how the defect it found stayed invisible for weeks.
   `360 560` is a KNOWN RED (177px below) and is deliberately not fixed — do not
   "fix" it by loosening the check.
6. **A 40-character sha comes out of `git rev-parse` or `git log --format=%H`,
   never out of a sentence.** Two guards refused reconstructed shas yesterday,
   in one session. `gh run list --commit <wrong sha>` answers `[]` with exit 0,
   which is byte-identical to "this commit has no CI" — a confident answer about
   nothing.
7. **`agent-status`'s verify-receipt gate may still read UNREADABLE for this lane.**
   The orchestrator shipped a resolver fallback on the night of 09-19; if you still
   see it, name it in ONE line and move on. Do not re-derive it, do not edit
   `cc-workspace.json` (it is written by `/api/cc/save-config`), and do not read the
   UNREADABLE as evidence a post failed.
<!-- hand:end banner -->

**What shipped (all pushed to `origin/main`; the four that changed something a reader
meets follow, plus `6312db3` report/changelog/this primer, `d522232` the
disposition-events log, and the close commit carrying `scripts/capture-frame.mjs` —
read its sha with `git log --oneline -1 -- scripts/capture-frame.mjs` rather than
trusting a number written here before it existed):**

- `b6d23b9` — **on a phone, every cycle page now shows its "Does it hold up?" answer
  without scrolling.** The Period / Reference peak / Paired data list moved out of the
  page header to sit against the curve that plots it. Measured at 390x664: the answer
  sentence ended 17px below the fold, and now ends 44px above it. No reader-facing
  words changed — `check-rendered-text.mjs` reports the same 86-line set in a different
  order. One component, so all ten cycle pages. CI green, live and verified serving.
- `d26a43f` — `scripts/measure-fold.mjs` plus the two frames it read
  (`docs/frames/2026-09-19-dalio-390-before.png` and `-after.png`, viewport-clipped at
  390x664 so the fold is where a reader's screen ends). Red arm verified at 360x560.
- `000e2f3` — W-001 read at n=19, Branch 0 holds, `nextEvaluation` 09-19 → 10-07.
- `90b39fe` — W-001's waits gate: the measured ~2.7/day rate and the pre-registered
  refusal to extend a third time.

**What a fresh agent would otherwise re-derive:**

- **The before/after frames are the evidence, and they are viewport-clipped, not
  full-page.** A full-page screenshot cannot show a fold. If you capture new ones,
  keep `fullPage: false` or the artifact stops answering the question it was made for.
- **`check-deployed-sha-drift --service sinusoidal-history` declines to judge this
  lane** — the service deploys on the commit trigger and that checker judges
  checks-gated services only. It is neither a pass nor a read. The delivery evidence
  that works here is a live text diff: `node scripts/check-rendered-text.mjs diff
  <a built-page snapshot> https://sinusoidalhistory.com/<path>`, which exits 0 when
  production serves what the build produced.
- **Engineering zero, read 2026-09-19 after the last push:** 0 open Dependabot alerts
  (`gh api "repos/u00dxk2/sinusoidal-history/dependabot/alerts?state=open&per_page=100"
  --paginate --jq "length"`), and `check-engineering-zero --project sinusoidal-cycles`
  reads `0 finding(s), 0 unreadable, nothing to waive`. No waiver is in force.
- **Nothing waits on David** and no board card is open for this lane
  (`answered-cards --project sinusoidal-cycles`, read 15:58Z).

**What NOT to touch:**

- **The banner region above.** It is rewritten in place at every close, never appended
  to, so it always states the current day's rules. The kickoff quotes it verbatim.
- **`scripts/measure-fold.mjs`'s 390x664 default.** It is the measured visible viewport
  of an iPhone 14 in Safari, not a guess, and the whole point is that it is smaller than
  the number the phone reports.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest and
  `verdicts.json` are manifest-frozen (`AGENTS.md`).
- **Frozen `public/data/state-2026.csv`** — as-published edition, deliberately carries
  the pre-`082ce4f` Turchin name.
- **The `--snapshot` series in `docs/crawl-reads.jsonl`** — Render serves only a rolling
  log window, so the snapshot is what makes a read diffable later.
