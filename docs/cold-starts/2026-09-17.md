# Cold-start primer for 2026-09-17

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-16 (first day on the daily rail)

<!-- hand:begin banner -->
**Read this before you touch anything.**

1. **This repo now has CI, and Render still ignores it.** `.github/workflows/ci.yml`
   runs lint, typecheck, test and build on every push. Render deploys on **every push
   regardless**, so a red CI can sit behind a live build. Check CI before you call a
   ship safe: `node ../skylark-site/scripts/check-ci-status.mjs --workflow ci.yml`.
2. **A pre-commit hook now scans staged lines for secrets.** If a commit is refused,
   read what it names — it blocks the commit, it does not "fail". Refresh
   `scripts/check-staged-secrets.mjs` only by re-copying skylark-site's file byte-exact.
3. **The zero you will see is not a failure.** Post-ship: 17 impressions, 0 clicks,
   average position 20.4. A page-two result at that n earns well under one click at any
   healthy rate. The CTR question is not answerable until ~2026-10-07. Do not "fix" the
   snippet again on this evidence.
4. **Do not change page titles, meta descriptions, H1s or URLs** without a reason that
   survives being written down. The indexing leg is still fresh, and a URL change is the
   one edit that would genuinely muddy it.
<!-- hand:end banner -->

**Today's first action** (the one dated gate, due 2026-09-19 — run it on the 19th, or
earlier if you want the trend):

```powershell
node C:/dev/skylark/sinusoidal-cycles/scripts/gsc-read.mjs --start 2026-09-10
```

It is a **sample-size check, not a verdict**: did the query set grow past the five
Dalio-dominated rows, and did average position move off ~20. W-001's own
`nextTrigger.action` says the same thing, and its notes carry the baseline.

**What shipped (all pushed, all on origin/main):**

- `6e41ff6` — `/methods` first screen: an "In brief" block (what the site is, a link to
  the chart, the spectral headline) and an "On this page" list. CI GREEN, live
  16:04:21Z, verified in production at 390 and 1440 wide. Counts derive from
  `cycles.json` / `series.json` / `verdicts.json` — never retype them into prose.
- `f9f2d47`, `831d100`, `114fe20` — vendored secret scanner + pre-commit hook + LF pin.
- `49a7658` — CI workflow and `repo-health.json`. Lane contract: `COMPLIANT`.
- `7f1267d`, `07273cf` — W-001 re-pointed at Search Console; `scripts/gsc-read.mjs` added.
- `37e5d30` — this lane's report of record moved to `docs/daily/<MT-date>-prelaunch.md`.

**The finding worth carrying:** the only search demand anyone can name for this site is
**Ray Dalio's "big cycle"** — 4 of the 5 queries Search Console will disclose, all at
positions 49–60, page 5–6. No wording change on our own pages reaches a position-55
result. Whether a ten-theory comparison site should compete for one theorist's named
theory is an open decision, surfaced at P3 (`edcf4fac`), not carded.

**Blocked on David:** GitHub's free security features — secret scanning, push protection
and Dependabot alerts — are all **disabled** on this public repo
(`gh api repos/u00dxk2/sinusoidal-history --jq '.security_and_analysis'`). Until they are
on, today's commit hook is the only layer, and dependency risk here is unreportable
rather than zero.

**Scoped, not built:** a per-cycle verdict strip on `/methods` — 9 rows from
`verdicts.json` (cycle · period · record length in periods · verdict · link), the
unpaired cycle marked. It needs a mapper and a frame read of a table at 390px.

**What NOT to touch:**

- **The `--snapshot` series in `docs/crawl-reads.jsonl`.** Render serves only a rolling
  log window; the snapshot is what makes a read diffable later.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest, `verdicts.json`
  are manifest-frozen (`AGENTS.md`).
- **Frozen `public/data/state-2026.csv`** — as-published edition, still carries the
  pre-`082ce4f` Turchin name on purpose.
- **Per-cycle titles at 76–82 characters** — they truncate from the brand suffix inward,
  which is the right thing to lose.

**Superseded from the 2026-09-09 primer:** its "Pending — David-side: one Search Console
read" line. That read happened on 2026-09-09, and as of 2026-09-16 the read is an agent
command, not a screenshot request.

## Session close-out — 17:40Z (appended at the close; supersedes two lines above)

Written after the P5 block above, and it contradicts that block in two places. Both
corrections below win; the section above is left standing as the record of what was true
at P5.

**SUPERSEDED: "Blocked on David: GitHub's free security features … are all disabled."**
Secret scanning and push protection were **enabled** by the orchestrator at ~16:47Z and
verified from this end (`gh api repos/u00dxk2/sinusoidal-history --jq '.security_and_analysis'`
→ `secret_scanning: enabled`, `secret_scanning_push_protection: enabled`); `repo-health.json`
corrected in `e3c97e7`. **Dependabot alerts remain OFF** (`gh api repos/u00dxk2/sinusoidal-history/vulnerability-alerts`
→ 404), so this lane still cannot report a dependency-risk count — `check-engineering-zero.mjs
--project sinusoidal-cycles` reads `NOTHING-SWEPT … unreadable, not clean`. The case for
turning alerts on is posted as `f7d253c8`; bounds-ledger, the standard being matched, has
them ENABLED (204) and carries zero dependencies to this repo's 638.

**ADDED: a second user-visible ship landed after the P5 block was written.** `e7d8936` —
every cycle page now answers "does it hold up?" above the curve: the verdict, the record
against the 3.0-period floor, how many more years of that measurement would reach it, and
that 0 of 9 pairings clear it. CI GREEN, live 17:12:05Z, verified by reading rendered
innerText on production for `dalio`, `kondratiev` (the differing-cut case) and
`turchin-fathers-sons` (no verdict row → no block, page renders). It was built under an
orchestrator ruling (`957e5e74`) with two limits: do not de-emphasise the other nine
cycles, and it is a content-quality ship, not rank mechanics.

## Session close-out — 17:45Z (post-close addendum; supersedes the Dependabot line below)

**FIRST ITEM TOMORROW: this repo has 46 open Dependabot alerts.** The orchestrator enabled
alerts on the public repo this afternoon (bus `75b79c2e`), after the close. **This is NEW
COVERAGE, not a regression** — the alerts were always true and the instrument was off.
Nothing about this code got worse; do not write it up as a first-rail-day defect, and do
not let it displace a product ship.

The trap, quoted from the gate because it has already cost another lane a false close:
`first_patched_version is the FIRST vulnerable range's patch, not yours — read
vulnerable_version_range per range, pick the range containing the INSTALLED major, verify
the LOCKFILE moved (walking-denver 9b46a82b: 2.4.5 offered, 3.1.6 needed)`. So: **close an
alert only when `package-lock.json` actually moved**, never because GitHub's suggested
version says it should have. A bump that does not move the lockfile leaves the alert true
and the row closed — the worst of both.

How to size it before touching anything: **group the alerts by ROOT package and report the
group count, not 46.** This repo has 27 direct dependencies and 638 resolved packages, so
46 alerts is almost certainly a handful of transitive roots. If it is two or three
`npm audit fix` roots, say so and do it. **If any one needs a major bump that changes the
build, that is a decision with its own row, not a rushed bump** — this repo's `npm test`,
`tsc` and `next build` all gate on that lockfile, and CI now runs all three on every push.

Either close them or file a dated `engineeringZeroWaiver` row in `continuity/items.json`
with a real reason and an `until` date; an unexplained red sits on the fleet panel nightly.

Start here: `gh api repos/u00dxk2/sinusoidal-history/dependabot/alerts?state=open --paginate`
then `node ../skylark-site/scripts/check-engineering-zero.mjs --project sinusoidal-cycles`.

**Still open for tomorrow, both owned elsewhere:**
- **Dependabot alerts — RESOLVED, they are ON.** Superseded by the addendum above: the ask
  in `f7d253c8` was answered by enabling them, which is what produced the 46-alert red.
- **The tier-1 instrument conflict is UNRULED.** `2655180f` (14:57Z) approved organic
  clicks by page as tier-1 with crawl-read demoted to monthly; `df65abc0` (15:06Z) said the
  orchestrator decides and crawl-read stays. W-001's `readCommand` currently reflects the
  approval. Revert in one command if the later message wins:
  `node ../skylark-site/scripts/continuity-edit.mjs --replace --set W-001 --field readCommand --value 'node scripts/crawl-read.mjs --days 14 --snapshot'`
- **Dependabot alerts** — see above.

**Open-redirect sweep (asked at the close):** no such finding was ever delivered to this
lane. Search space: this lane's whole `tmp/.bus-events.jsonl`, pattern `open.?redirect`,
case-insensitive → **1 hit, and it is the close-session message itself** (eventId
`97e85f24`, 17:31:12Z). Read my own routes rather than the list: `next.config.ts` has ONE
redirect, and its destination is the hardcoded literal `https://sinusoidalhistory.com/:path*`
selected by a `host` match — no user-controlled destination. `grep` of `src/` for
`redirect|NextResponse.redirect|permanentRedirect|searchParams.get` returns 4 hits, none of
them a redirect sink (`/api/v1/state` parses a year; `/og` reads cycle/peak/period to draw
an image). **No open redirect exists on this site to close.**
