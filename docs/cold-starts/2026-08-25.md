# Cold-start primer for 2026-08-25

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-08-24 ~21:20Z (journey-walk round 1)

### Today's load-bearing ships (all live-verified on production)

- `e0b8422` — **Journey-walk round 1: 19 reader-experience fixes** from the
  first cold walk of the whole journey since Phases 13–15 (two cold-reader
  agents, desktop + mobile touch, + the 8 mechanical detectors over six
  pages). Findings doc: `docs/journey-walk-2026-08-24.md`. Includes the
  31-junction **JSX-whitespace sweep** — this Next version eats
  element-adjacent spaces in multi-line text nodes ("the 2026reading",
  "estimationconventionally"); hazard now documented in AGENTS.md and
  continuity KP-003.
- `48d494f` + `75e5212` — the focus-scroll landing chain: the headline
  finding was that "tap/click a row to focus" rendered its result
  ~800–1,200px below the fold and read as a **broken control** on both form
  factors. Final form: manual `scrollTo` asserted twice (layout settles
  after the effect — a single scroll, smooth OR instant, lands ~700px
  short; instrumented live). Verified facetTop=12 on mobile tap, mobile
  deep-link, desktop click.
- `cc0a5fb` — final verification record in the walk doc.

Everything stayed inside the GSC-window guardrails: **no titles, meta
descriptions, H1s, URLs, data CSVs, or spectral outputs touched.**

### Tomorrow's first-action

**No dated work exists before 2026-09-03** (the W-001 GSC indexing read).
If a session runs anyway, the highest-value move is putting the four
deliberate deferrals from the walk in front of David as one decision batch
(see `docs/journey-walk-2026-08-24.md` § "Deliberately not fixed"):

1. Mobile "one axis": the Overlay tab is `display:none` on phones while
   the H1 + cycle-page CTAs promise it. Reword after the GSC window, ship
   a mobile overlay, or accept.
2. Calibrate tab shows no chart (you calibrate blind) — layout decision.
3. Overlay tab defaults to all 19 lines on — needs a curated default set.
4. Post-9/03 batch: dedup the five double-suffix `<title>`s ("Poster ·
   Sinusoidal History · Sinusoidal History" on /poster /methods
   /embed/docs /about /colophon) + the OG card's hardcoded "No. 01".

A 30-second real-phone eyeball of the focus tap on https://sinusoidalhistory.com
is worth it (verified in emulation only).

### CI

No CI workflows in this repo (deliberate; `docs/daily-config.md` records
check-ci-status as a no-op here). Tree clean, HEAD `cc0a5fb` pushed.

### What NOT to touch tomorrow + why

- Titles/metas/H1s/URLs — frozen until the 9/03 GSC read (mid-indexing).
- The frozen spectral surface (manifest, verdicts.json, figures) and every
  data CSV — manifested; any CSV change forces a 99,999-draw re-freeze.
- The em dash inside cycle `name` fields — seven splitters now
  (`cycleRoutes.ts:62` feeds URLs).
- When writing ANY prose in TSX: element-adjacent spaces must be explicit
  `{" "}` (AGENTS.md, KP-003) — verify against rendered HTML.
- `public/state-2026.csv` + source.md, v1 API — frozen edition surfaces.

### Watch items maturing

None within 24h. **W-001 → 2026-09-03**: GSC coverage read for the new
domain (indexing + first impressions; crawl presence already answered
8/21 with 731 Google-family hits). Second variable in that window: the
first inbound link went live on skylarkcreations.com 8/23 — attribute
carefully.

### Evangelism / binding-constraint read

Binding constraint is still **discovery** (W-001 wait state). The
would-tell-a-friend moment — "this chart made a contested idea legible
enough to argue with" — is exactly what today's session served: the focus
interaction that delivers the legibility read as *broken* to every cold
reader before today, and now lands the reader on the rationale + caveat +
calibration in one gesture. Both cold walkers independently rated the
substance (focused facet, cycle pages, reuse paths) as excellent once
reached — the product's problem was delivery order, and the worst of it is
fixed. Retention/word-of-mouth: still unmeasurable by design (no
analytics; GSC impressions become the first instrument on 9/03). No
real-user advocacy signal today (no channel exists yet to carry one).
