# Cold-start primer for 2026-09-26

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — written 2026-09-25 morning (for the day 2026-09-25)

<!-- hand:begin banner -->
**Read this before you touch anything.**

### First action — read the one open first-screen gap, then decide whether it or `W-002` is the day's ship

Every entry page now answers on a 390x664 and a 360x560 phone's first screen: 13 of 13
at both, read on production after `27ec7df`, 2026-09-25 ~8:20 AM MT
(`docs/daily/2026-09-25-prelaunch.md`). The only open first-screen gap is the
smallest phone. This command reads it (10 of 13 on production at the time of writing):

```
node scripts/check-entry-folds.mjs --width 320 --height 568
```

`/cycles` is 2px short, `/cycles/turchin` 28px and `/methods` 40px. Closing it takes
one more type step below 390px or a change to the 103px site header. That header is
shared with the home page, which has only **4px** spare at 360x560, so re-read `/` at
360x560 after any header edit. `W-002` (does a phone reader understand a cycle's
confidence tag) and `I-006` (cycle counts written as literals) are dated 2026-09-27,
the day after this primer's.

### Carry-forwards

1. **`scripts/check-entry-folds.mjs` is the instrument now; run it at both phone sizes
   after ANY edit above an entry page's answer.** It walks 13 legs from one
   `{question, path, selector}` table, so a new cycle adds a leg with no edit. The count's
   denominator is its leg count, never a hand count (`docs/daily-config.md`, "The two
   leading product numbers"). It is blind to an answer clipped by an `overflow:hidden`
   ancestor, and it reads the home leg HIDDEN at 640px and up (the phone overview is
   `sm:hidden`), which is correct.
2. **Every phone-only class on `/cycles/<slug>`, `/cycles` and `/methods` has an `sm:`
   twin that restores the desktop value.** That is how desktop stayed identical on
   2026-09-25, checked at 1024x768 and 640x800 against production. The H1s moved from an
   inline `style` clamp to a `sm:text-[clamp(...)]` class with the same values; keep
   the twin when you edit either.
3. **The thinnest legs:** `/` 4px and `/cycles/turchin` 12px at 360x560. Turchin has the
   longest description on the roster; a copy edit there costs rows first.
4. **`next dev` rewrites `AGENTS.md`** (it inserts its own agent-rules block and dropped
   the JSX-whitespace section on 2026-09-25). That rewrite is uncommitted and left for
   David. Before committing anything, check `git status` for `AGENTS.md`, and never
   stage it by accident. `next build` and `next start` do not do this.
5. **`I-004` and `I-005` are closed** (`412d2ba`); the two numbers live in
   `docs/daily-config.md`.
6. **A row's spoken reading must move with its drawn one** (`src/lib/overviewReading.ts`,
   pinned by its red-armed test). Do not re-introduce a second copy of "is the current
   year inside the range".
7. **`check-rendered-text.mjs diff` cannot see the home page's chart** (client-rendered).
   For anything inside `Viz`, the proof is the rendered `main` innerText line multiset.
8. **`W-001`'s clock is 2026-10-07.** Titles, meta descriptions, H1 text and URLs stay
   frozen everywhere until that read. Layout and body copy are in scope. Do not run the
   Search Console read looking for news; it lags ~3 days.
9. **In Git Bash, a bare `/cycles` argument is path-mangled** into
   `C:/Program Files/Git/cycles`. Pass URL paths to scratch scripts from PowerShell, or
   pass full URLs.
<!-- hand:end banner -->

**What shipped on 2026-09-25 (all on `origin/main`, CI green on each code commit, live on Render):**

- `45abf72` — `scripts/check-entry-folds.mjs` and its spawned selftest.
- `5838ca8` — the fathers-and-sons page answers "Does it hold up?"; every cycle page
  answers at 390x664.
- `27ec7df` — every entry page answers at 360x560.
- `f71bd60`, `412d2ba`, `f8fac09` — ledger corrections, `I-004`/`I-005` closed, the two
  numbers declared.

**What NOT to touch:**

- **The banner region above**: it is rewritten in place at every close, never appended
  to, and its `### First action` heading is load-bearing (the hook reads it).
- **`src/lib/overviewReading.ts` and its test.**
- **`scripts/measure-fold.mjs`'s defaults and output strings**: they are compared across days.
- **Spectral verdict surfaces** (`public/data/spectral/`, the manifest, `verdicts.json`),
  which are manifest-frozen (`AGENTS.md`).
- **Frozen `public/data/state-2026.csv`**, the as-published edition.
