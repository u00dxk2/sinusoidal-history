# Cold-start primer for 2026-08-15

Multi-session primer — each `/close-session` run appends a new section below.

---

## P2 continuity — 2026-08-15 — GPTBot crawled all 8 Phase 12 routes

**Read the day's gate six days early and it had already fired.** The 08-14 section
below says "no crawler has fetched a `/cycles*` page yet." That is now **false**, and
it is the single most important correction in this file.

`node scripts/crawl-read.mjs --days 7 --snapshot`, run 2026-08-15 15:33Z:

- **2026-08-15 10:27:04Z–10:27:38Z — GPTBot content-crawled the whole Phase 12
  surface**: `/`, `/cycles`, **all eight** `/cycles/<slug>` routes, and all eight
  `?focus=<id>` deep links.
- **10:54Z — GPTBot came back for RSC navigation payloads**
  (`/?_rsc=…&focus=perez`, `/cycles?_rsc=…`). It was traversing the app's client
  routing, not just fetching static URLs from the sitemap.
- **Latency, first sitemap exposure → content crawl: ~43 hours** (sitemap listed the
  routes 08-13 15:32Z; crawl landed 08-15 10:27Z, across two intervening daily
  sitemap polls). **A new route on this site gets LLM-crawled in about two days.**

So the Phase 12 ship is validated end-to-end, and the old worry that `/cycles` needed
a home-page link to escape crawl-budget triage was **unfounded** — don't act on it.

**Google is still at ZERO** in the same 7-day window, and that half is unchanged.
The zero has a positive control: the same classifier recorded 67 GPTBot + 9
OAI-SearchBot hits in that window, the buckets sum exactly to the 2401 raw log
lines, and `--selfcheck` unit-asserts the `Googlebot` / `Google-Extended` branches —
so a Google hit would have been recorded. It is a measured zero, not a dead probe.

### The number in this file that will lie to you

The read also shows **1481 "browser-ish"** and **778 "unknown (no UA)"** lines
(~323/day). **That is not a human audience read and must not be reported as one.**
`browser-ish` is a UA-*shape* fallback bucket — anything not a known crawler, not
empty, not our own tooling — and portfolio rule is that attribution verifies IP
ranges, never UA strings alone. On a site with zero Google presence and no inbound
links, ~323/day of browser-shaped traffic is far more likely scanners and scrapers
with spoofed UAs. **The human-residual question is OPEN**; answering it needs
reverse-DNS / IP-range work, which is a bigger change than reporting the bucket.

### What changed in the tracking files

- `continuity/items.json` — W-001's signal date moved from nested `nextTrigger.date`
  to **top-level `nextEvaluation`**, and `meta.signalDateField` now declares it. The
  nested date made this item **invisible to every portfolio overdue-gate scanner**:
  `collectSignalDates()` does a flat `item[declared]` lookup, so a dotted path
  resolves to `undefined` and the item read as date-less while the dashboard showed
  green. That blindness is why a gate that fired on 08-15 was scheduled to be noticed
  on 08-21.
- `docs/crawl-reads.jsonl` — **new**. One aggregate JSON line per read. Render's log
  API only serves a trailing window, so findings expire; this is the series that
  survives. Take a snapshot on every real read.

## P3 — 2026-08-15 — two beliefs refuted, one live defect fixed

### Fixed: four pages were telling engines they were duplicates of the home page

Verified live before and after. `/about`, `/methods`, `/colophon`, `/poster` all
served `<link rel="canonical" href="https://sinusoidal-history.skylarkcreations.com">`
— the home page. Those are exactly the pages GPTBot content-crawls.

Root cause: the root layout set `alternates.canonical: "/"`, and Next inherits
metadata down the tree, so every route that didn't override it adopted the home page
as its canonical. `/cycles` and `/cycles/<slug>` escaped only because Phase 12 set
theirs explicitly. **The fix removed the root default**, so a route that forgets now
emits no canonical and self-canonicalizes (harmless) rather than inheriting a wrong
one (harmful). Wrong-by-default was the bug; four patches would not have fixed it.
`c456fc5`.

### Refuted #1: the llms.txt + markdown-mirror strategy is NOT being consumed

The 08-14 section below says the crawl read "vindicates the `llms.txt` +
`.md`-mirror strategy." **It does not.** In 7 days and 77 crawler hits — including a
full GPTBot content crawl that took `/about`, `/methods` and `/colophon` **in HTML** —
**no crawler fetched `/llms.txt` or any prose `.md` mirror.**

Prove-the-zero: `/llms.txt`, `/about.md`, `/methods.md` all return **200**, so this
is not a 404 artifact; and one `.md` *was* fetched in-window
(`/data/us_world_gdp_share.source.md`, by an unidentified bot), proving `.md` paths
are fetchable and recorded. What the crawl read actually vindicates is **the HTML
pages and the sitemap**.

**Consequence — do not build more markdown surface.** Generating `.md` mirrors for
the 8 cycle pages is the obvious-looking next move and the evidence says nobody would
fetch them. The three existing mirrors carry a standing sync burden (AGENTS.md has an
invariant about it, and one commit had to fix three drift incidents at once) for a
surface with zero recorded reads. Re-check on the next crawl read before deciding
whether to retire them.

### Refuted #2: there is no referrer in the server logs

Checked the raw Render log entry directly. Labels: `resource, method, statusCode,
host, path, level, type`. Message: `clientIP, requestID, responseTimeMS,
responseBytes, userAgent`. **No `Referer`.** So "did a reader move from a cycle page
into the chart?" and "did anyone arrive from a shared link?" are **not answerable
server-side**, and the referrer-as-share-proxy idea floated in P1 is dead. Any
share/traversal measurement here needs an instrument that does not exist yet.

### Shipped: the chart's first link into the per-cycle pages

`grep "/cycles/" src/components/` returned **nothing** — the overlay had no route
into the 8 per-cycle pages, while those pages all linked into the chart. One-way
traversal. Added a "why this peak? read the rationale →" link in `CalibrationPanel`,
at the point of intent (someone dragging the reference-peak slider). Covers 7 of 8 —
`CalibrationPanel` requires a paired series and Perez has none. `d9c9f2f`.

## Session close-out — 2026-08-14 (day rail: /daily kickoff + a design pass)

### Today's load-bearing ships

- **`f35e4d1` — `scripts/crawl-read.mjs`, and the finding it produced.** W-001 had
  been carried as "zero analytics, blocked on a David decision about Google Search
  Console." That premise was **too strong**: Render's HTTP request logs already record
  path + user-agent for every hit, so the *crawl* half was answerable with no
  instrumentation, no client script, and no consent surface on a site that
  deliberately has none. `--selfcheck` covers the UA-classification ordering and the
  label/message parsing offline.
- **`3f285d5` + `b07872c` — accessibility and mobile mechanics.** A
  `/design-detectors` + `/usability-audit` pass. Detector findings **161 → 64**.
  Full write-up: `docs/design-canon-review-2026-08-14.md`.

### The finding that should shape what comes next

The 7-day crawl read (2026-08-07 → 08-14) says:

- **Zero Google hits of any kind.** Not Googlebot, not Google-Extended. Google does
  not appear to know this site exists.
- **GPTBot and OAI-SearchBot poll `/robots.txt` and `/sitemap.xml` daily**, unbroken,
  ~15:2x UTC. GPTBot ran a full content crawl on 08-12 over `/`, `/about`,
  `/methods`, `/colophon`, `/poster`, `/embed/docs`.
- That crawl **predates the Phase 12 ship by one day**, so it saw none of the 8
  `/cycles/*` routes. GPTBot has since re-read the sitemap (08-13 15:32Z).
  ~~**No crawler has fetched a `/cycles*` page yet** — that is the open question.~~
  **SUPERSEDED 2026-08-15 — see the section below. It has now.**

Read: the working discovery channel here is **LLM crawlers, not search**. That
vindicates the `llms.txt` + `.md`-mirror strategy and is the first evidence this
project has ever had about who wants it. It also means *more SEO surface is not the
move* — Google's absence is a discovery problem (no inbound links), not a ranking one.

### What the design pass actually found

The editorial design is strong and **was not changed**. What sat underneath it:

- The page **scrolled sideways at 390px** (nav forced `scrollWidth` 399 > 375,
  clipping "About" off the edge).
- **`/methods` was unreachable from the nav at every width** — the link carried
  `hidden xs:inline`, but `xs` is not a configured breakpoint in this project, so the
  variant never applied and only `hidden` took effect. Latent for months.
- 32 tap targets under the 44px floor → 1. 49 uses of 9–10px text → the 11px floor.
  Focus was invisible site-wide (no `:focus-visible` rules; two components set
  `focus:outline-none`). Chart annotations 4.48:1 → 5.84:1.

### Known false positive — do not "fix" it again

The detector's **19 surviving `low-contrast` findings are a confirmed FP.** Its
pixel-sampler reads the chart gridlines/curves *behind* the SVG text, so it reports
1.1:1 for text whose real composited ratio is 5.84:1. Measured in-page, not inferred.
If a future run flags these, verify with composited-color math before touching
opacity — raising it further only adds noise to the chart. Written up for the
allowlist owner (skylark-site) in the review doc.

### Tomorrow's first action

Nothing is blocked and nothing is half-built. The highest-leverage next moves, in
order:

1. **Get one inbound link from a page Google crawls often.** This is the actual
   unblock and it is a David action — an Uncaged Minds post is the obvious one
   ("I built a cycle-theory site whose main argument is that cycle-theory sites are
   wrong"). The GitHub repo description + 12 topics were fixed 08-14; that repo page
   is currently the site's only crawlable inbound link.
2. **GSC property** — still a David yes/no. Ten minutes, and it converts "Google
   doesn't know us" from inference to fact.
3. **Re-run the crawl read on 08-21** (W-001's new trigger date, pulled in from
   09-13). See the branching in `continuity/items.json`.

If you are here for engineering work: there is still no open engineering need. Do
not invent one.

### Pending decision-blocked items

- **W-001** — narrowed, not closed. The crawl half no longer waits on David; the
  impressions/queries/ranking half still needs GSC. Re-dated to **2026-08-21** with
  concrete branching. Raise the GSC ask at a natural touchpoint, not standalone.

### Active in-flight work

None. No Codex jobs, no open branches. Working tree clean, HEAD pushed.

### CI

Still **no CI** — no `.github/workflows/`. If you ship here you run
`npm run build` + `typecheck` + `lint` + `test` yourself, then fetch production
after Render auto-deploys. Nothing else will.

### What NOT to touch

Unchanged from 2026-08-14, and still binding:

- **`cycles.json` / `series.json` prose** — four external fact-check rounds. Any
  year-phase claim change requires `python scripts/audit_cycle_rationales.py` first.
- **Cycle `id` values** — an id determines a public URL since Phase 12.
- **The prose mirrors** (`public/about.md`, `methods.md`, `colophon.md`) — edit in
  the same commit as their React counterparts.
- **`public/llms.txt` per-cycle list + the `about.md` mirror block** — the two
  surfaces a new cycle does NOT update automatically.
- **New:** `src/components/ui/tabs.tsx` is vendored shadcn. The 44px tap floor is set at the call
  site in `Viz.tsx` on purpose; don't push it into the primitive.

### Binding-constraint status

**Discovery — and as of today we can see part of it.** Not "nothing is measurable"
any more: crawl is measurable and instrumented (`scripts/crawl-read.mjs`), and it
says LLM crawlers are the live channel while Google is absent entirely. The
human-facing half — impressions, queries, whether a page was ever shown to a person
— still needs a Search Console property that does not exist. Retention, sharing, and
word-of-mouth remain unmeasurable, and that is fine for a reference artifact.

### Supersedes

`docs/cold-starts/2026-08-14.md` — specifically its "no analytics of any kind" framing
and its "next evaluation 2026-09-13" date for W-001, both of which are now wrong.
