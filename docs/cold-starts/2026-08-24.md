# Cold-start primer for 2026-08-24

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-08-23 ~12:40 MT (light close, side lane)

### The one thing that happened: the site got its FIRST inbound link

skylarkcreations.com now cross-links Sinusoidal History. David asked for it
mid-morning; the request went to the orchestrator over the bus with drop-in
copy and placement, and the orchestrator shipped it the same hour as
skylark-site commit `985e023f`. David eyeball-confirmed it live.

Placement, as David specified: the **featured-reading section on the
skylarkcreations.com homepage, beside the Magnifica Humanitas card** — a
`sm:grid-cols-2` wrap, eyebrow "Featured project", external `<a>` to
https://sinusoidalhistory.com with `target="_blank"` — plus a sibling footer
link. Deliberately **NOT** in the `#projects` grid and **NOT** in
`src/data/projects`; that was his call, not a shipping shortcut, so do not
"fix" it later by moving it into the app grid.

Nothing in THIS repo changed. The work was entirely skylark-site's; the only
local artifact is the memory update below.

### Why this matters for W-001 (2026-09-03 GSC read)

Until today the site had effectively zero inbound links — that absence is the
whole reason the pre-8/20 "Google absent" reads were a *discovery* problem
rather than a ranking one. There is now exactly one inbound link, live from
2026-08-23, and it is followable from a homepage. **When the 9/03 GSC read
lands, that is a second variable in the window**, not just the 8/20 sitemap
submit. Attribute movement carefully; don't credit the sitemap for a referral
path that didn't exist a week earlier. Recorded in the
`project_discovery_channel` memory with the date so the start is checkable.

### Repo state

`origin/main` unchanged by this session apart from this primer and a
`continuity/lint-history.json` health-snapshot line. No code, data, prose, or
spectral outputs touched — so no cos-math audit, no mirror sync, no manifest
concerns apply.

### Customer impact today

A person browsing skylarkcreations.com can now find and click through to
Sinusoidal History, which was impossible yesterday.
