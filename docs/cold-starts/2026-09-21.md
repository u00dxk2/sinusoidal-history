# Cold-start primer for 2026-09-21

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 2026-09-20

<!-- hand:begin banner -->
**Read this before you touch anything.**

1. **FIRST ACTION — walk the home page `/` at 390x664 as a cold arrival.** This read
   has now found a real fold defect on two consecutive days, on surfaces that looked
   fine on every desktop and in every full-page screenshot. The front door has not
   had it, and it carries the chart this site exists to show.

   ```
   node scripts/capture-frame.mjs https://sinusoidalhistory.com/ docs/frames/2026-09-21-home-390.png 390 664
   ```

   That exact command ran twice on 09-20 against `/cycles`; only the URL and the
   output path change. **Open the PNG before writing a word about it.** `fullPage`
   is false on purpose — a full-page screenshot cannot show a fold, which is the
   trap both of this week's defects hid in. The question is narrow: does a phone
   reader reach the chart, or does the fold cut before it?

2. **`check-rendered-text.mjs diff` is an ORDER gate, and on a re-order it goes RED
   and is RIGHT to.** This cost real time on 09-20 and the lesson is worth the
   paragraph. `firstDifference()` compares visible text line-index by line-index,
   so any re-ordering reports RED at the first moved line. **Do not read that RED as
   "the page broke", and — more dangerous — do not learn to wave it through**, or a
   genuine prose deletion inside a future re-order rides out on the habit. What
   proves a re-order safe is the line MULTISET: `snap` production and
   `.next/server/app/<page>.html`, sort both, compare. On 09-20 that was 216 lines
   and 166 distinct on both sides, identical. Note the same command run
   BUILT-vs-PRODUCTION after a deploy is a delivery proof and *should* be GREEN —
   same tool, different question.

3. **W-001's clock is 2026-10-07 and its question CHANGED on 09-20.** It is no longer
   click-through. The waits gate found the denominator cannot fill — ~2.7
   impressions/day projects to ~68 against a floor of 100 — so the row was CONVERTED
   rather than re-dated: the 10-07 read asks whether the query set grew beyond the
   five Dalio-dominated rows and whether average position moved off ~21. Click-through
   reopens as a fresh row only if position moves under 20. **Do not run the Search
   Console read looking for news before then** — Search Console lags ~3 days, so daily
   re-reads return byte-identical totals, which is the waste that date exists to stop.

4. **Titles, meta descriptions, H1s and URLs stay frozen** until W-001 reads —
   everywhere, not just on the page you are editing. The freeze protects the
   comparison the row rests on, and both converted questions are still snippet- and
   rank-sensitive, so it holds with the same 10-07 expiry. **Layout and body copy are
   in scope**, which is what 09-20's ship used.

5. **Two rows come due 2026-09-27, one week out.** `W-002` — does a phone reader
   understand a cycle's confidence tag now the glossary follows the roster; its
   readCommand captures the frame but **cannot answer the question**, which is a
   comprehension judgment, and the row says so. `I-001` — the workspace-slug gap;
   skylark-site's to fix, relayed, **do not edit `cc-workspace.json` from this lane**
   and do not re-derive the diagnosis, it is complete in the row.

6. **Do not re-try a `title={gloss}` tooltip on the `/cycles` confidence tag.** It was
   tried and reverted inside P4 on 09-20 for two measured reasons: hover-only, so it
   does nothing for the phone reader the question is about; and the extra element
   SPLITS that line in `check-rendered-text`'s extraction, permanently perturbing the
   page's text baseline. The reason is recorded in the component. If the 09-27 read
   asks for an affordance, the named fix is to split the entry's click target so the
   tag can be a real anchor to `#confidence-tags`.

7. **A 40-character sha comes out of `git rev-parse` or `git log --format=%H`, never
   out of a sentence.** `gh run list --commit <wrong sha>` answers `[]` with exit 0,
   byte-identical to "this commit has no CI" — a confident answer about nothing.

8. **`agent-status`'s verify-receipt gate still reads UNREADABLE for this lane, and a
   second registry has the same gap.** `check-next-primer-exists.mjs` exits 2
   UNRESOLVABLE because REPORT_CONFIGS has no row under `sinusoidal`, the mirror of
   cc-workspace having none under `sinusoidal-cycles`. Both are on `I-001`. Name it in
   ONE line and move on; do not read the silence as a failed post.
<!-- hand:end banner -->

**What shipped (all pushed to `origin/main`):**

- `2d693ac` — **on a phone, `/cycles` now shows the cycles.** It used to open with the
  header, a nine-line paragraph on how the curves are drawn and six lines of tag
  glossary, with **zero of the ten entries** above the fold — an index whose index was
  below the fold. New order under the H1: the spectral verdict sentence with "How the
  test works →", the ten entries, "Does any of them hold up?" with its table, then the
  drawn-paragraph and glossary. The table went BELOW the roster deliberately: its
  Verdict column is off-screen at 390 (min-width 36rem), so leading with it would have
  refilled the fold with a clipped table. No reader-facing words changed. CI green,
  serving, delivery proven by a built-vs-production text diff.
- `2fb6785` — the production 390x664 frame the reached-claim rests on. Four frames are
  committed in total: `2026-09-20-cycles-390.png` (before), `-390-after.png`,
  `-390-after-prod.png`, plus the `-1440-after.png` and `-360-after.png` extremes.
- `cc1dfda` — `I-001` opened after three days unrowed.
- `f4a72ba` — W-001 converted, W-002 opened, the reverted tooltip's reason recorded.

**What a fresh agent would otherwise re-derive:**

- **The confidence tag is NOT undefined.** `src/app/(app)/cycles/[id]/page.tsx:247-253`
  renders `Confidence classification: <LABEL> — <gloss>.` on every per-cycle page, which
  is the destination each index entry already links to. Journey-walk J8's "defined
  nowhere on the site" is no longer an accurate description; what changed is ORDER on
  one page. W-002's open question is narrower than J8's was.
- **`check-deployed-sha-drift --service sinusoidal-history` declines to judge this lane**
  — commit-trigger service, and that checker judges checks-gated services only. Neither
  a pass nor a read. The delivery evidence that works here is
  `node scripts/check-rendered-text.mjs diff <built page> https://sinusoidalhistory.com/<path>`,
  GREEN when production serves what the build produced.
- **Engineering zero, read 2026-09-20 after the last push:** 0 open Dependabot alerts
  (`gh api "repos/u00dxk2/sinusoidal-history/dependabot/alerts?state=open&per_page=100"
  --paginate --jq "length"`), and `check-engineering-zero --project sinusoidal-cycles`
  reads `0 finding(s), 0 unreadable, nothing to waive`. No waiver in force.
- **Nothing waits on David** and no board card is open for this lane.

**What NOT to touch:**

- **The banner region above.** Rewritten in place at every close, never appended to, so
  it always states the current day's rules. The kickoff quotes it verbatim.
- **`scripts/measure-fold.mjs`'s 390x664 default and its red 360x560 case.** The default
  is the measured visible viewport of an iPhone 14 in Safari, not the 844px the phone
  reports — and that gap is the entire defect class this week. The red case is red on
  purpose.
- **The `/cycles` section ORDER**, and the comment block at the top of that component
  explaining it. It is the 09-20 judgment-read cut, and the reasoning for the table's
  position is there so nobody "tidies" it back above the roster.
- **Spectral verdict surfaces** — `public/data/spectral/`, the manifest and
  `verdicts.json` are manifest-frozen (`AGENTS.md`).
- **Frozen `public/data/state-2026.csv`** — as-published edition, deliberately carries
  the pre-`082ce4f` Turchin name.
- **The `--snapshot` series in `docs/crawl-reads.jsonl`** — Render serves only a rolling
  log window, so the snapshot is what makes a read diffable later.
