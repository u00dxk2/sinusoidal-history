# Cold-start primer for 2026-08-30

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 19:30Z (2026-08-29 ~13:30 MT)

Short ad-hoc session: one cosmetic ship, no gates opened or closed.

**Today's load-bearing ships**
- `082ce4f` — Turchin fathers-and-sons cycle renamed `Turchin (50y) — fathers-and-sons cycle`. The pre-em-dash byline is rendered alone by 7 splitters (State table, home summary panel, CycleFacet mobile, OG card, cycle-page fallback, `cycleTheorist()`), so the two Turchin cycles read as "Peter Turchin" vs bare "Turchin" — distinct but not self-explaining (journey-walk J15 had only patched this on mobile with a period chip). `id`/URL unchanged; `about.md` mirror + CHANGELOG updated; frozen `state-2026.csv` deliberately left as published per its freeze contract. 79 tests, typecheck, lint pass. Known cosmetic residue: the mobile summary row now reads "Turchin (50y) · 50y" (byline + J15 period chip) — harmless, drop the chip on that row only if David minds.

**Tomorrow's first-action**
- Nothing is due 8/30. The next dated gate is **2026-09-03**: the W-001 GSC indexing read (`node scripts/crawl-read.mjs --days 14 --snapshot` + GSC coverage/impressions on David's account). Do not run it early unless two more crawl windows have clearly passed — the 8/21 read already answered "is Google present" (yes, 731 hits).
- If the session has no dispatch, the deferred queue (below) is the only sanctioned work.

**CI:** no CI — repo has no `.github/workflows`; verification is local (vitest 79, tsc, eslint, all green at `082ce4f`).

**What NOT to touch**
- GSC-window guardrails until the 9/03 read: no titles, metas, H1s, URLs, or data changes. (Today's rename is a `name` field + below-the-fold byline, not a title/meta/URL — deliberately inside the guardrail.)
- Spectral verdict surfaces: manifest-frozen. Never hand-edit `public/data/spectral/`, the manifest, or verdicts.json (AGENTS.md).
- Frozen `public/data/state-2026.csv` — as-published edition; it now intentionally carries the old cycle name.

**Deferred (named gates, carried from 8/25 primer)**
- Title-dedup decision — gated on the 9/03 GSC read.
- Mobile-overlay decision, calibrate-tab chart, overlay default — David-preference calls, no date.
- Spectral-figure fonts — ride the next HATCH re-freeze.

**Watch items maturing tomorrow:** none. W-001 `nextEvaluation` = 2026-09-03.

**Binding-constraint / evangelism read:** unchanged — discovery. Both channels alive (LLM crawlers daily; Google arrived 8/20 within hours of sitemap submit, 731 hits by 8/21). Impressions/queries — the first-ever who-wants-this evidence — land at the 9/03 GSC read. No retention/word-of-mouth instrument exists (reference site, no accounts); would-tell-a-friend moment remains "this chart made a contested idea legible enough to argue with."
