# Cold-start primer for 2026-09-09

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 03:30Z (2026-09-08 ~21:30 MT)

Listener-only session that turned into one thing: the overdue W-001 gate was read for
the first time ever, and it came back with real news. **No product code shipped.**

**Supersedes** the 2026-08-30 primer's "next dated gate is 2026-09-03" line — that gate
has now been read (5 days late; no session ran here 08-30 → 09-08, nothing blocked it)
and re-dated to 2026-09-19.

**Today's load-bearing work** (no product ship; one bookkeeping commit — see gap note below)
- **The W-001 crawl read executed through the continuity gate for the first time since the
  row was created 2026-08-13.** It had never run. The first attempt was killed at the 60s
  default cap with 78 bytes; the cause is not a slow script but Render's 30 req/min logs
  API — a 14-day read paginates ~250 pages and `crawl-read.mjs` backs off on 429 by design,
  so the backoff waits *are* the runtime. Set `readCommandTimeoutMs: 600000` on the row;
  the re-run took 548s, exit 0, `complete: true`, 25,215 log lines, snapshot #3 appended
  to `docs/crawl-reads.jsonl`.
- **Findings folded into `continuity/items.json` (W-001 notes) and agent memory.**

**What the read actually said** — three things worth knowing before you touch anything:

1. **Google held and deepened.** Googlebot 375 hits/14d, up from 188 in the 08-21 window,
   and it is content-crawling now rather than onboarding: 9 of the 10 `/cycles/<slug>`
   routes (all but `modelski`), all 8 paired CSVs at 5 fetches each, the spectral SVGs.
   Google-family *total* fell 731 → 447 **only** because the one-off GoogleOther
   onboarding sweep ended (543 → 48). Do not read that total as a decline — the
   sustained-crawl number doubled.
2. **Bing arrived on its own, at Googlebot parity.** Bingbot 373 hits/14d, absent from
   every prior read, and it crawled **all 10** cycle routes plus `/cycles`, `/methods`,
   `/poster`, `/about`, `/embed/docs`, `/state/2026`. There is no Bing Webmaster Tools
   property — this is organic off robots.txt + sitemap.
3. **The `llms.txt` / `.md`-mirror ban from 2026-08-15 is overturned.** That entry
   concluded "do not build `.md` mirrors for the cycle pages" because nothing had ever
   fetched one. In this window Googlebot fetched `/llms.txt` (3×), `/methods.md` (2×),
   `/colophon.md` and `/about.md`, and OAI-SearchBot fetched **every** `.source.md`.
   The surface is consumed by both a search engine and an LLM crawler. This does not
   make per-cycle `.md` mirrors a good idea automatically — the AGENTS.md sync burden is
   real and unchanged — but it is now an open question to argue, not a settled no.

**Instrument caveat you must carry — the per-crawler totals are partly spoofed.** A single
vulnerability scan (2026-08-28 08:28–08:31Z, 643 requests) **rotated crawler user-agents**,
and the UA classifier believed them: 68 "Meta/Facebook", 57 "OAI-SearchBot", 55 "GPTBot",
51 "Amazonbot", 27 "Applebot", 26 "PerplexityBot", 23 "Bytespider", 20 "Google-Extended",
19 "ClaudeBot". That is 83% of Google-Extended's entire 14-day total and ~60% of Meta's
and Bytespider's. Something calling itself GPTBot asked for `/api-keys.json`.
**Read `coverage`, never `byWho`** — the coverage map is keyed on real site paths and
excludes the 404 probes, which is why findings 1–3 above still stand. By coverage the
genuine content-crawlers number **six**, not eight: Googlebot, Bingbot, OAI-SearchBot
(deepest — every cycle route, every `.source.md`, all three `/api/v1` endpoints), GPTBot,
Amazonbot (40 distinct paths incl. all 10 cycle routes), Applebot. Real but shallow:
PerplexityBot (6 paths), Meta (6 — but it took `/llms.txt` and all three `.md` mirrors),
ClaudeBot (3). Noise: Bytespider (`/robots.txt` only) and Google-Extended (**zero** real
paths — its coverage map is empty).

And the long-open "are the `browser-ish` lines humans?"
question is answered **no**: the snapshot's path list shows those 18,572 + 3,395 lines
are dominated by vulnerability scanning (`/.env` and ~40 variants, `/wp-config.php`,
`/secrets.json`, `/terraform.tfstate`, `/root/.aws/credentials`). Not an incident —
static Next.js, none of those paths exist — but never report that bucket as readers.

**Tomorrow's first-action**
- **There is no agent-runnable gate due.** The derived owed list is empty (below), and
  re-running the crawl read would burn 9 minutes to re-answer a question answered today.
- If you want product work, the honest queue is the deferred list below. The single
  highest-value *thinkable* item is now the `.md`-mirror question re-opened by finding
  (3) — but do not ship it on a cold start without arguing the sync burden first.

**Pending — David-side, carded on his board**
- **One Search Console read, which only he can do**: index coverage for the 11 URLs
  (`/cycles` + 10 per-cycle routes) and any impressions/queries under Performance. This
  is the *only* remaining leg of W-001 and request logs can never answer it — logs show
  crawling, never impressions, queries, position, or index state. ⚠ requires David at
  the pane (his Google account, hello@skylarkcreations.com).
- **Optional, same shape**: verify a Bing Webmaster Tools property, now that Bing is a
  live channel at Google parity and just as unmeasured.

**Watch items maturing tomorrow** — derived, not authored
(`node ../skylark-site/scripts/check-due-gates-dispositioned.mjs --print --today=2026-09-09`):

```
due-gates: 0 gate(s) due on/before 2026-09-09 (read-only; no snapshot written)
  0 due — nothing listed (verify will pass trivially, which is correct).
```

**CI:** no CI — this repo has no `.github/workflows`; `gh run list` returns empty.
Verification here is local (vitest, tsc, eslint). Nothing was built or tested today
because no source file was touched.

**What NOT to touch tomorrow**
- **The GSC-window guardrails are now RELAXABLE but not relaxed** — they existed to keep
  the 9/03 read interpretable. That read has happened. Still, do not change titles,
  metas, H1s or URLs on a cold start without a reason; the indexing leg is still
  outstanding and a URL change mid-indexing is the one edit that would genuinely muddy it.
- **Spectral verdict surfaces are manifest-frozen.** Never hand-edit
  `public/data/spectral/`, the manifest, or `verdicts.json` (AGENTS.md).
- **Frozen `public/data/state-2026.csv`** — as-published edition; it intentionally still
  carries the pre-`082ce4f` Turchin cycle name.
- **`readCommandTimeoutMs: 600000` on W-001** — do not lower it. Shorten `--days` instead.

**Deferred (named gates, carried forward)**
- Title-dedup decision — was gated on the 9/03 GSC read; now gated on David's coverage read.
- Mobile-overlay decision, calibrate-tab chart, overlay default — David-preference calls,
  no date.
- Spectral-figure fonts — ride the next HATCH re-freeze.
- Per-cycle `.md` mirrors — **newly re-opened** by today's finding (3); no date, needs an
  argument on sync burden before it is work.

**Binding-constraint / evangelism read**
- Constraint is still **discovery**, and it has moved in the right direction without
  anyone doing anything: two search engines and eight AI crawlers now consume the site,
  where a month ago there was one channel and Google was at literal zero.
- **The love read remains UNMEASURED and structurally so** — this is a reference site with
  no accounts, no return loop, and no analytics; retention, sharing and word-of-mouth
  cannot currently be observed at all. The would-tell-a-friend moment is unchanged:
  "this chart made a contested idea legible enough to argue with."
- No real-user advocacy signal this session. There has still never been evidence that a
  *human* has loaded this site — and today's read moved that question from "open" to
  "the traffic we can see is bots and scanners." The GSC impressions read is the first
  instrument that could answer it.

**Bookkeeping-commit gap note:** the commit carrying this primer, the W-001 ledger update
and the regenerated `continuity/health.json` is not listed among today's work above — it
is the commit that carries this list. It is titled `docs(close-session): …`, which is an
exempt subject shape for `continuity-check.mjs`.
