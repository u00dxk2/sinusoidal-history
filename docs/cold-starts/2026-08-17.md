# Cold-start primer for 2026-08-17

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 19:10Z (2026-08-16, MT morning — bus-dispatch lane only)

**Read this first: today was a thin day.** One dispatch consumed, one docs commit,
plus a continuity/dead-reference cleanup. No product code changed. Do not go looking
for a feature ship in today's log — there isn't one, and the 2026-08-15 primer is
still the substantive read for where this project actually stands.

### Today's load-bearing ships

- **`700d3cd`** — closed the orchestrator's CI-truth dispatch (queued 2026-08-15
  22:22Z, consumed ~15h later). Added the `check-posted-unpushed.mjs` command to
  `docs/daily-config.md` and recorded that `check-ci-status.mjs` is a deliberate
  no-op here. Ran clean: 0 findings, every commit this project ever cited in a
  task-complete reached origin.

### Tomorrow's first-action

**Nothing is queued and nothing is blocked.** The honest first-action is to pick up
the thread the 2026-08-15 primer names, not to re-derive state from today's thin log.

If you want a concrete opener: run the crawl read and see whether GPTBot came back
after the 8/15 canonical fix (`c456fc5`) — that commit stopped six pages declaring
themselves duplicates of the home page, and no crawl has been taken since it landed.
That is a real, cheap measurement of whether the fix changed anything.

```powershell
Set-Location C:\dev\skylark\sinusoidal-cycles
$env:RENDER_API_KEY = [System.Environment]::GetEnvironmentVariable('RENDER_API_KEY','User')
node scripts/crawl-read.mjs --snapshot
```

Compare against the 2026-08-15 baseline in `docs/crawl-reads.jsonl`: 78 crawler hits
over 7 days, GPTBot on all 8 `/cycles/*` routes, **googleHits: 0**.

### CI status

**No CI.** This repo has no `.github/workflows/` — nothing runs on push, and there is
no CI verdict to cite, ever. A ship here means running the gates locally
(`npm run build; npm run typecheck; npm run lint; npm test`) and verifying production
after Render auto-deploys. Today's commits were docs-only, so no gates were run — say
that plainly rather than implying a green build.

The other half of the CI-truth pair **does** apply and is now documented in
`docs/daily-config.md`:

```
node ../skylark-site/scripts/check-posted-unpushed.mjs --project sinusoidal-cycles
```

### What NOT to touch tomorrow + why

- **`check-ci-status.mjs`** — do not go hunting for a workflow file to point it at.
  Its absence here is deliberate and now documented; adding CI to a
  four-doc static site is not the constraint.
- **The prose mirrors** (`public/about.md`, `methods.md`, `colophon.md`) — edit in the
  same commit as their React counterparts. Non-negotiable; three drift incidents.
- **`public/llms.txt` per-cycle list + the `about.md` mirror block** — the two surfaces
  a new cycle does NOT update automatically.
- **`src/components/ui/tabs.tsx`** is vendored shadcn. The 44px tap floor is set at the
  call site in `Viz.tsx` on purpose; don't push it into the primitive.
- **Analytics beyond crawl-read** — PostHog/GSC remains a David-voice call (it adds a
  privacy surface to a site that has none). See W-001; don't unilaterally instrument.

### Watch items maturing

**W-001** (P2, monitoring) — zero human-side analytics; the Phase 12 SEO routes cannot
be measured for impressions. `nextEvaluation` is the top-level signal field (this was
the 8/15 fix — a nested `nextTrigger.date` made it invisible to every overdue scanner).
Today I linked three commits to it (`539ec7d`, `b4e04a2`, `0d4d705` — the crawl-read
instrument and the item's own plumbing). Not closeable without a GSC property.

### Binding-constraint status — the evangelism read

- **The one core problem, in the user's words:** "I keep hearing that history runs in
  cycles — Kondratiev, Turchin, Strauss-Howe — and I can't see them. Show me the
  actual curves against the actual data, and tell me where the claim is weak."
- **The would-tell-a-friend moment:** the chart snaps to a named cycle and the user
  sees a real series underneath it with its provenance one click away — the moment
  they realize this is sourced, not vibes. Distance from it: **the moment exists and
  is live; nobody has verified a human ever reached it.**
- **Retention / word-of-mouth / reviews:** not instrumented, and deliberately so — this
  is a reference artifact with no accounts, no cookies, no consent surface. There is
  no retention number to report and standing one up is not obviously the move.
- **Real-user advocacy signal this session:** none. No reviews, referrals, or shares.
  The only observed audience is machines.
- **Discovery (the actual binding constraint):** crawl is measurable and instrumented;
  the human half is not. As of the 8/15 read, **LLM crawlers are the live channel and
  Google is absent entirely (googleHits: 0)** — GPTBot fetched all 8 per-cycle routes
  ~43h after the sitemap went up. More SEO is not the move; being legible to crawlers
  already works. Whether a *person* ever saw a page is still unknowable.

### Housekeeping done today (so you don't rediscover it)

- Continuity was WARN (7 untracked commits) → now **OK**. Three linked to W-001; four
  given rationales in `meta.untrackedCommits`.
- Dead-reference check was FAIL (3) → now **PASS**. All three were path-precision
  issues, not missing files: the shadcn tabs primitive is now cited by its full
  path `src/components/ui/tabs.tsx`, and two
  cross-repo refs now written as `../skylark-site/src/lib/...`.
- `continuity/lint-history.json` carried an uncommitted 8/15 `STANDING_PATTERN_CONTRADICTION`
  record from a prior session; committed as bookkeeping.

### Supersedes

Nothing. Today added no read that overrides the 2026-08-15 primer — treat that file as
the current substantive state of this project.
