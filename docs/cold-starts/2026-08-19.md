# Cold-start primer for 2026-08-19

Multi-session primer — each /close-session run appends a new section below.

## Session close-out — 21:40Z (2026-08-18 MT)

The single biggest day in this project's history: five site deploys, six research
reports ingested, a 4-lens adversarial review absorbed, the roster expanded 8→10,
Zenodo archival with a DOI, and both domains purchased. Everything below is
live-verified on production.

### Today's load-bearing ships (all pushed, tree clean)

- `9c80742` — Phase 13: `/state/<year>` annual reading + v1 JSON API
  (`/api/v1/cycles|series|state?year=`, CORS-open).
- `ef367b9` + `c737df9` — the two adversarial-review fix waves: 4 cos-math prose
  errors that survived four prior external fact-check rounds, license/citation
  corrections, one phase classifier (deleted the wide-band one), URL-param
  hardening (`?period.x=0` used to NaN the chart), Project Mars tooltip shows raw
  units again, UTC year everywhere, `/state` force-dynamic (cached-404 class).
- `a8e7daf` — Zenodo DOI folded in. v1.1.0 is the first archived release;
  **concept DOI 10.5281/zenodo.21998618** (version DOI …21998619).
- `e897c1e` — Phase 14: TEN cycles (added Modelski 110y + Turchin fathers-and-sons
  50y; Perez paired with HATCH; "Considered and excluded" list on /cycles with
  primary-source-verified citations; Kondratiev periodicity caveat; full count
  sweep incl. all mirrors).
- `f529468` — /state verdict ritual: deterministic headline ("In 2026, 3 of the 10
  constructions read at peak, and 8 of 10 sit above their midline"),
  what-this-does-not-mean, frozen edition CSV `/data/state-2026.csv`.
- `90cc1b2` — **`docs/specs/spectral-verdict-build-spec.md`** — the pre-registered,
  warm-context spec for the next big build. Everything is in there: protocol,
  adjudications, gate table, test suite, session-start checklist.
- skylark-site: `7fb918fc` (6 research raws + 3 syntheses ingested) and `37508b85`
  (queue metabolization columns updated).

### Tomorrow's first-action (pick by whether David is present)

1. **Domain cutover to sinusoidalhistory.com** — David bought BOTH
   `sinusoidalhistory.com` and `sinusoidalhistory.org` on 2026-08-18 (renew Aug
   2027). ⚠ requires David at the pane — DNS records live at his registrar (and
   possibly the Render dashboard for the custom-domain add; try the Render API
   with `RENDER_API_KEY`, service `srv-d7mcat7lk1mc73bidim0`, first). Agent side of
   the cutover: Render custom domain + cert, permanent 301s from
   `sinusoidal-history.skylarkcreations.com` (the proven GPTBot channel follows
   redirects — 301s stay forever), `SITE_URL`/`SITE_DOMAIN` in
   `src/lib/siteConfig.ts`, then a sweep of the HAND-WRITTEN absolute URLs:
   `public/llms.txt`, the three markdown mirrors (`about.md` has per-cycle
   `**Page:**` lines), `README.md`, `CITATION.cff` `url:` field. Zenodo metadata
   updates on the NEXT release, not retroactively. The frozen
   `/data/state-2026.csv` + its source.md stay as published (frozen means frozen;
   old URLs 301). Decide .com-primary vs .org-primary with David (recommendation
   given: either works; the other 301s).
2. **If David absent: the spectral verdict build** — start at
   `docs/specs/spectral-verdict-build-spec.md`, follow its session-start checklist
   verbatim (then the research synthesis in skylark-site). Ungated, fully specced,
   expected headline "0 of 9 paired constructions reach the eligibility gate."
3. **Also approved and ungated: reuse packets** on cycle pages (download-CSV +
   PNG/SVG export + copy-paste attribution + visible license, adjacent to each
   chart). David said yes on 2026-08-18. Good bundle-mate for either session.

### Sequencing rule (from W-001, updated today)

Domain cutover FIRST, then GSC verification on the NEW domain, then sitemap
submit. Do NOT create a GSC property on the old subdomain — that verifies twice.

### Pending decision-blocked items

None on the board. (GSC needs David's Google account but is deliberately
sequenced behind the cutover; the cutover's DNS half needs David at the
registrar — both are scheduled work, not open decisions.)

### Active in-flight work

None. All background agents, Codex jobs, and workflows from today completed and
were consolidated. Zenodo webhook is live on the repo (future releases
auto-archive).

### CI status

No CI workflow on this repo (`gh run list` returns empty — noted, not a red).

### What NOT to touch tomorrow + why

- **`public/data/state-2026.csv` + its source.md** — frozen edition, never
  recomputed, that's its entire value proposition.
- **The v1 API surface** — FROZEN per the citability research (both engines):
  no new endpoints. And **do not build an MCP server** — killed pre-build by the
  same research (registries are agent tool-calling, not reference discovery).
- **Fact-checked prose (`cycles.json` rationales, mirrors)** — any year-phase
  edit requires `python scripts/audit_cycle_rationales.py` + same-commit mirror
  sync (AGENTS.md).
- **`cycles.json` file ORDER** — home-page "Cycle No. NN" numbering follows it;
  new cycles append at the end.
- **GPTBot crawl volume as a KPI** — retired by research; proxy hierarchy is
  chatgpt.com referrals > ChatGPT-User > OAI-SearchBot depth.

### Watch items maturing tomorrow-ish

- **W-001** `nextEvaluation` 2026-08-21: re-run
  `node scripts/crawl-read.mjs --days 14 --snapshot`; if Google still zero at 14d,
  the unblock is the two-step above (cutover → GSC on new domain), already teed up.

### Evangelism / binding-constraint read

Reference/explainer artifact; the would-tell-a-friend moment is "this chart made a
contested idea legible enough to argue with." Binding constraint is still
DISCOVERY: Google at a measured zero, GPTBot crawling daily (~43h sitemap-to-crawl
latency for new routes — expect /cycles/modelski, /cycles/turchin-fathers-sons,
/state/2026 crawled by ~Aug 20). The citability stack after today: DOI, dated
annual verdict with a deterministic quotable number, frozen edition CSV, ten
fact-check-hardened cycle pages, a published excluded-list (rare honesty surface).
Retention/word-of-mouth still unmeasurable — GSC after cutover is the instrument,
and the research prescribes a 60–90-day log baseline before attributing anything.
No real-user advocacy signal today (nothing measurable exists yet).
